# 한의원AI로컬구축 — macOS 네이티브 앱

상태: SwiftUI 네이티브 전환 진행 — SOAP 분할 시험 구현/로컬 후보·부분 적용 확인, 임상 품질 미달·개발자 서명 전환·재빌드 권한 유지 확인, Cloud Pro 사용 한도 도달 · 기존 웹/DB/AI 기능 보존 · 갱신: 2026-09-17

이 문서는 사용자가 수용한 UX 검토를 반영한 현재 제품 명세다. 아래 요구사항을 기준으로 구현하고 원샷 실행 loop에서 검증한다. 구현 여부와 실제 시험 결과를 구분하며 수용 기준별 최신 판정은 이 문서와 `history.md`의 최종 평가를 함께 확인한다. 실제 검증 결과는 `history.md`에서 관리한다. 이전 UX 시험은 단위/HTTP 51개와 DOM·인앱·Apple 실제 엔진을 포함했다. Cloud 확장 최종 검증은 65개 단위/HTTP·전체 DOM·인앱과 동일 합성 입력 12회 실제 생성이다. 자세한 실패·수정·재시험 결과는 history.md에서 구분한다. 부정 표현·시점 표현의 모델 오류가 남아 있어 임상 내용의 100% 정확성을 충족했다는 뜻은 아니다. 폐기된 설계와 조사 기록은 Git 이력에서 확인한다.


## macOS 네이티브 전환 — 실행 명세

- 목표: 진료 전 확인 → 실시간 문진 → SOAP 편집 → 안내 준비를 네이티브 Mac 앱에서 수행한다.
- 범위: SwiftUI/AppKit 앱, 기존 로컬 Python/SQLite API 재사용, AVAudioEngine 마이크, 파일 가져오기, 모델 선택·후보 적용·편집/이력·설문/검사 입력.
- 완료 조건: N1 실제 .app 빌드/실행, N2 웹뷰 없는 창·사이드바·툴바·Liquid Glass·Inspector, N3 데이터 읽기/자동 저장/충돌·이력 복구, N4 세 모델의 기존 작업/후보 흐름, N5 네이티브 마이크·임시/확정 전사·파일 입력, N6 설문/검사/방문/안내 업무, N7 키보드·다크/축소 창·오류/종료·기존 DB 보존 검증.
- 제외: 기존 웹/DB 삭제, 환자 자료 자동 Cloud 전송, 오디오 녹음 파일 생성, App Store/외부 배포·공증, 새 동기화·상주 로그인 서비스.
- 검증: 최종 macOS 27 SDK arm64 Release 빌드/서명 확인, Swift 로직/실제 API 회귀, 승인된 합성 DB 네이티브 GUI 시험. 빌드 성공과 실제 업무 검증을 구분한다.

### 플랫폼과 실행

이 Mac 전용 macOS 27.0+/Apple Silicon 로컬 앱이다. bundle ID는 `com.raphael.ClinicAI`, 이름은 `한의원AI로컬구축`이다. 소스는 `macos/`, 빌드 산출물은 `.build/native/한의원AI로컬구축.app`으로 둔다. 고정 Apple Development 서명을 사용하며 Mac App Store/App Sandbox 배포를 약속하지 않는다. 마이크 사용 설명을 포함한다. 기존 Python/단축어/Swift 브리지 사용 때문에 이번 개인 로컬 앱은 sandbox 밖에서 실행한다. 광범위한 권한이나 로그인 항목은 추가하지 않는다.

직원 화면은 SwiftUI NavigationSplitView, 시스템 List/Toolbar, SF Symbols, Inspector, native text editor·undo, 파일 패널, 메뉴·단축키로 만든다. WKWebView/HTML 화면을 포함하지 않는다. Liquid Glass는 탐색·도구 계층에 적용하고 본문은 불투명한 읽기 표면으로 유지한다. 기본 구성은 왼쪽 환자 목록 + 중앙 진료 문서 + 필요 시 오른쪽 참고 자료다. 검색·진료 단계·생성/마이크 조작은 상단 툴바와 연결한다. 자동 저장/실시간 전사는 앱 전체를 잠그지 않는다. 활성 마이크는 대상 환자를 명시한다.

기존 API/SQLite를 현재 데이터 권위로 유지하며 SwiftData 사본이나 별도 환자 DB를 만들지 않는다. 초기 전환에서는 실행 중인 localhost 서비스를 사용하며 앱에서 서비스 시작을 지원한다. 실제 DB는 기존 `.build/qa-case/clinic.sqlite3`, 검증은 별도 `.build/native-qa/clinic.sqlite3`다. 데이터 이관·삭제 없이 앱 전환을 검증한다. 새 앱을 닫을 때 미저장 편집·진행 중 마이크를 조용히 폐기하지 않는다.

### Xcode 프로젝트 실행

- 열기: `macos/ClinicMac.xcodeproj`, 공유 scheme `ClinicMac`, 실행 대상 `My Mac`. `Package.swift`를 MCP workspace로 열지 않는다.
- Xcode 프로젝트 기본 실행은 합성 시험 DB/8768이다. 원본 진료 DB로 전환하지 않는다.
- 빌드: `xcodebuild -project macos/ClinicMac.xcodeproj -scheme ClinicMac -derivedDataPath .build/xcode-native build`. 기존 `scripts/build_macos.sh`도 정식 Xcode 도구 경로로 변경했다.
- Swift Testing은 현재 Swift package target으로 실행한다. 시험 API가 8768에서 실행 중일 때 `CLINIC_NATIVE_INTEGRATION=1 xcrun swift test --package-path macos --scratch-path .build/native-tests`를 사용한다. Xcode app scheme의 Testables는 아직 비어 있어 RunAllTests로 이 검증을 대신하지 않는다.
- MCP는 현재 세션에서 `xcrun mcpbridge` JSON-RPC 클라이언트로 연결했다. Codex 전역 config에는 아직 상시 MCP 서버 등록을 추가하지 않았다.

### 디자인 근거

Apple HIG, Apple 공식 macOS 27 Design Resources 및 사용자가 제공한 Sketch/Figma 키트를 참조한다. 시스템 제공 재질/컨트롤을 실제 SDK에서 컴파일하여 확인하며, 고정 스크린샷/가짜 창 장식/웹 CSS로 흉내 내지 않는다. 초기 구현 시 전체 Xcode는 26.5 SDK, 별도 CLT는 27 SDK/Swift 6.4였다. CLT로 네이티브 앱 실행까지 확인했다. Xcode 27.0(27A266a) 설치 후 정식 SDK/Swift 6.4로 빌드·테스트를 재개했다. Xcode MCP에서 ClinicMac.xcodeproj를 열고 BuildProject 성공까지 확인했다. 시스템 Xcode 선택은 임의로 바꾸지 않는다. 상세 검증과 잔여 작업은 history.md N 절을 따른다.

## 1. 목표와 범위

- 목표: 환자를 연 뒤 검사·문진·SOAP 수정·환자 안내 준비까지 하나의 진료 작업 화면에서 끝낸다.
- 범위: 탐색 구조, 오늘의 진료 목록, 환자 요약, 설문·검사 입력, 전사, AI 결과 검토, 안내 준비, 자동 저장·복구, 재진·과거 기록을 3단계로 개선한다.
- 완료 조건: 정상 진료 흐름에서 별도 DB 메뉴 이동, 같은 환자 재선택, 생성한 메시지의 수동 옮겨 적기를 없애고 사용자 작성 내용을 보존한다.
- 제외 범위: 외부 메시지 발송, Notion 동기화, Sigma 쓰기, 자동 모델 fallback, 마이크 오디오 파일 생성.
- 검증: 동일한 합성 사례로 개편 전후 작업 흐름을 비교하고, 단계별 수용 기준·회귀 테스트·실제 인앱 화면·필요한 Apple 엔진 시험을 구분해 확인한다.

## 2. 제품 원칙과 보존 조건

1. 단일 사용자 Mac 로컬 MVP를 유지한다. 기존 Python 서버·SQLite·Apple 엔진을 기반으로 개선한다.
2. 화면은 환자와 현재 진료 작업을 중심으로 구성한다. DB 테이블과 관계 설정은 앱이 처리하고, 필요한 관리 기능은 보조 화면에 둔다.
3. `진료 전 / 문진 / 기록 검토 / 환자 안내`는 자유롭게 이동하는 작업 공간이다. 순서 강제나 완료 잠금으로 사용을 막지 않는다.
4. 동의·검토·승인·발송일 상태는 초안 생성·로컬 저장을 차단하지 않는다. 누락과 주의점은 해당 작업 가까이에 표시한다. 요청 출처·입력 형식·관계 무결성·수정 충돌 검사는 유지한다.
5. AI는 버튼을 누를 때만 실행한다. 자동 저장이 생성 요청이나 외부 전송을 유발하지 않는다. AI 결과는 검토용 초안이며 생성 성공을 임상 정확성으로 해석하지 않는다.
6. 마이크 음성은 메모리·파이프로만 처리한다. 오디오 파일·Blob·MediaRecorder를 사용하지 않는다. 기존 파일 업로드 전사와 텍스트 입력을 유지한다.
7. 사용자 수정본, 전사 원문과 출처·시간 정보·이력을 보존한다. 재생성·화면 이동·오류가 기존 내용을 조용히 덮어쓰지 않는다.
8. 기존 DB·시험 자료를 초기화하지 않는다. 1·2차 개편에서 기존 7개 테이블의 의미와 관계를 유지하며 필요한 저장 구조만 확장한다. 구조 변경은 백업·마이그레이션·복구 검증을 포함한다.
9. Notion 문서는 구조·임상 스킬의 참조 원천이다. 현재 제품 동작은 이 PRD를 따른다. 공개용 JSON의 스키마·스킬 의미를 보존하고 로컬 프롬프트와 출력 형식을 별도로 맞춘다.

## 3. 개편 출발점과 개선 결과

개편 전 MVP는 설문→세션 자동 생성, 검사 연결, 가/나/다 생성, 수동 SOAP 편집, 파일·마이크 전사, CRM 로컬 기록을 제공했다. 전사 원문·시간 정보·이력과 revision 충돌 검사가 있다. 아래 차이를 개편의 출발점으로 삼는다.

| 개편 전 | 개편 요구사항 | 요구사항 |
| --- | --- | --- |
| 7개 DB 중심 메뉴, 소개 배너와 통계 중심 홈 | 다음 작업을 바로 시작하는 진료 목록 | UX-01 |
| 세션 첫 화면에 제목·관계·관리 속성 노출 | 환자·주소증·검사 요약과 핵심 행동 우선 | UX-02 |
| 가·전사·나·SOAP·다가 긴 페이지에 배치 | 작업별 공간과 요청 시 여는 참고 자료 | UX-02, UX-05 |
| 검사 입력 시 연결할 세션을 직접 선택 | 열린 진료에서 자동 연결해 검사 입력 | UX-03 |
| 마이크 사용·AI 생성 중 전체 편집/이동 잠금 | 문진 중 메모, 생성 중 다른 작업 지원 | UX-04, UX-07 |
| 수동 저장, 재생성 시 해당 결과 교체 | 자동 저장·버전 복구·결과 비교 후 적용 | UX-05, UX-07 |
| CRM 요청은 수신자만 채우고 메시지는 수동 입력 | 안내문·메시지 결과를 바로 편집·복사 | UX-06 |
| MVP 화면과 미동의 시 초안 금지 프롬프트가 불일치 | 화면·서버·AI 지침이 같은 비차단 원칙 적용 | UX-08 |
| 설문 기반 세션 구조, 환자별 재진 연결 없음 | 환자와 방문을 구분한 과거 기록·경과 | UX-09 |

기존의 35개 단위/HTTP 테스트와 DOM·실제 전사 시험 통과 기록은 이전 MVP의 근거다. UX 개편의 통과 증거로 대신하지 않는다.

## 4. 탐색 구조와 기본 화면

주 메뉴는 `오늘의 진료`, `진료 기록`, `환자 안내`, `자료실`, `설정`으로 정리한다. 자료실에는 한의학 자료와 읽기 전용 참조 문서를, 설정에는 안내 템플릿·운영 체크·로컬 AI 상태와 사용 안내를 둔다. 3차에서 진료 기록을 환자별 방문 목록으로 확장한다.

기존 설문·검사·CRM 데이터는 삭제하지 않는다. 환자 진료 화면의 요약·입력·연결 기록으로 접근하며 상세 관리 경로도 유지한다. 기존 세션 링크는 같은 기록으로 연결되어야 한다.

```text
오늘의 진료 목록 │ 환자 · 나이 · 주소증        저장 상태   [마이크 시작]
                │
대기            │ 진료 전 | 문진 | 기록 검토 | 환자 안내
진료 중         │
기록 정리       │ 현재 작업 공간             │ 참고 자료
완료            │                            │ 설문·검사·전사
```

참고 자료는 필요할 때 열고 좁은 화면에서는 접는다. 항상 3열을 강제하지 않는다. 환자와 방문, 저장 상태, 활성 마이크 상태는 작업 공간을 바꾸어도 식별 가능해야 한다. 세션명·담당자·관계 수정·삭제 등 관리 작업은 상세 메뉴에 둔다.

## 5. 상세 요구사항

### UX-01. 오늘의 진료와 탐색 — 1차

- 소개 배너보다 진료 목록을 먼저 표시한다. 기본 작업 보기에는 대기·진료 중·기록 정리·완료를 사용하고 보류도 식별 가능하게 한다.
- 환자명, 주소증, 접수 시각 또는 대기 시간, 진행 상태, 이어서 할 작업을 보여준다. 대기 시간은 접수 시각 기준 경과로 표시하며 진료 시간으로 혼동하지 않는다.
- 이름·주소증 검색과 상태 필터를 제공한다. 통계나 상태 항목을 누르면 해당 조건의 목록으로 연결한다.
- `문진 이어가기`, `기록 검토`, `안내문 확인`을 해당 작업 공간으로 바로 연결한다. 안내는 권장 행동이며 다른 작업을 막지 않는다.
- 기존 상태값은 유지한다. 표시 그룹은 `접수·설문확인·검사확인→대기`, `문진중→진료 중`, `AI문서작성·한의사검토·안내준비→기록 정리`, `완료→완료`로 대응하고 `보류`는 별도 표시한다. 화면을 열었다는 이유만으로 상태를 변경하지 않는다. 전사·검사·AI 결과의 준비 여부는 주 진행 상태와 별도로 표시한다.
- AI 모델 상세·운영 설명은 보조 정보로 옮긴다. 실제 기능 오류는 해당 행동 옆에서 확인할 수 있어야 한다.

### UX-02. 환자 진료 작업 화면 — 1차

- 첫 화면에서 환자·방문 식별, 주소증, 주요 설문 내용, 최신 검사 요약, 다음 행동을 확인한다.
- 상단의 `진료 전 / 문진 / 기록 검토 / 환자 안내`를 자유롭게 이동한다. 같은 환자와 방문 맥락, 진행 중 입력을 유지한다.
- 진료 전에는 설문·검사 요약과 AI 참고, 문진에는 입력·메모, 기록 검토에는 SOAP·근거, 환자 안내에는 전달할 결과물을 배치한다.
- 내부 관계명·DB 입력값·스킬 실행 지시문을 일반 진료 흐름에 노출하지 않는다. 관리·원문 열람 기능은 상세 보기로 제공한다.
- 대표 버튼은 현재 작업에 맞는 구체적 이름을 사용한다. `가·나·다`는 상세 정보에 남기고 기본 표시는 `진료 전 요약 / SOAP 초안 / 환자 안내`로 한다.

### UX-03. 설문과 검사 입력 — 1차

- 설문은 기존 10문항의 의미·저장 매핑과 질문 순서를 보존하면서 읽기 쉬운 표현으로 바꾼다. 예: `주소증`→`오늘 가장 불편한 증상`, `발병 시점 또는 경과`→`언제부터, 어떻게 달라졌나요?`.
- 복용약은 `없음 / 있음`을 먼저 선택하고 있음일 때 상세 입력을 제공한다. 미입력을 자동으로 없음으로 저장하지 않는다. 기존 자유 입력 답변도 손실 없이 편집할 수 있어야 한다.
- 형식 오류는 해당 항목 옆에 표시하며 제출 실패 후 이미 입력한 답변을 보존한다. 현재 개인정보 동의의 MVP 선택 처리도 유지한다.
- 식별번호 수집 범위·필수 여부 변경은 이번 문구·배치 개선에 포함하지 않는다.
- 열린 진료에서 검사 입력을 시작하면 해당 세션을 자동 연결한다. 연결 대상은 화면에 표시하고 매번 다시 선택하게 하지 않는다.
- 혈압 수축기/이완기를 붙이고 맥박·체온에 단위를 표시한다. 기본 측정값, 추가 검사, 진찰 메모를 입력 순서대로 묶는다.
- 미검사·미입력과 실제 숫자 0을 구분한다. 검사 추가·수정 후 같은 진료 작업 화면에서 요약이 갱신된다.

### UX-04. 문진·전사·메모 — 1차 배치, 2차 동시 작업

- `마이크 / 녹음 파일 / 텍스트 입력`을 같은 문진 공간의 입력 방식으로 제공한다. 파일·텍스트 기능을 제거하지 않는다.
- 마이크 버튼, 경과 시간, 입력 감지, 전사 처리 상태, 정지·취소를 일정한 위치에 표시한다. 입력 감지는 음량·장치 입력 여부이며 인식 정확도나 진단 확신도로 표현하지 않는다.
- 2차에서는 마이크 전사와 동시에 의사 메모·진찰 소견을 입력하고 저장할 수 있다. 전사와 의사 입력은 구분해 AI에 제공한다.
- 작업 공간 전환으로 마이크가 자동 종료되거나 다른 환자의 전사로 바뀌지 않는다. 마이크는 시작한 세션에 고정하고 대상 환자를 계속 표시한다. 다른 환자로 전사 대상을 바꾸려면 현재 전사를 명시적으로 종료한다.
- 중간 결과와 확정 전사를 구분한다. 정지 후 확정 텍스트·시간 정보는 기존 전사 뒤에 저장하고 원문 이력을 유지한다. 취소는 이번 마이크 전사를 반영하지 않으며 별도로 저장한 의사 메모는 보존한다.
- 연결·권한·엔진 오류 때 마이크 해제 여부와 남은 텍스트를 분명히 보여주고 저장·복구 경로를 제공한다. 사용자가 취소한 전사가 다음 방문에 자동 복원되지 않아야 한다.
- 음성 파일 미생성 원칙은 자동 저장·복구에도 동일하다. 복구 대상은 텍스트와 메타데이터이며 오디오가 아니다.

### UX-05. AI 결과와 기록 검토 — 1차 표시, 2차 버전·적용

- 진료 전 요약은 주소증·경과·검사·확인할 내용을 구분해 보여준다. 긴 Markdown 원문은 필요할 때 열고, 기본 화면은 읽기 쉬운 형식으로 렌더링한다.
- SOAP는 S/O/A/P를 각각 편집한다. 기존 SOAP 문자열·편집본은 마이그레이션 시 보존하고 확실히 나눌 수 없는 기록은 원문 확인 상태로 남긴다.
- SOAP 옆에서 설문·검사·전사를 확인한다. 참고 자료는 사실·사용자 메모·AI 해석의 출처를 구분한다. 문장별 근거 링크를 제공하면 실제 원문 구간과의 연결을 검증하며 근거가 없으면 만들지 않는다.
- 수치·부정 표현·미검사 등 주의점을 관련 내용 가까이에 표시한다. 원문 대조를 쉽게 하되 경고 자체를 생성·저장 차단으로 사용하지 않는다.
- 2차 재생성은 별도 후보를 만들고 현재 편집본과 비교해 적용한다. 항목별 선택 적용·현재 내용 유지·이전 버전 복구를 제공하고 사용자 수정본을 자동으로 덮어쓰지 않는다.
- 원자료 변경 후에는 어느 결과가 이전 입력으로 생성되었는지 표시한다. 오래된 결과도 읽고 복사할 수 있으며 최신이라고 표시하지 않는다.
- SOAP 복사와 텍스트 내보내기는 화면에서 선택·적용한 최신 편집본을 사용한다. 앱의 복사를 외부 EMR 저장 완료로 기록하지 않는다.

### UX-06. 환자 안내와 메시지 — 1차

- 메뉴명은 `환자 안내`로 한다. `환자 안내문 / 처방 설명 / 메시지`를 각각 읽고 수정·복사할 수 있게 한다.
- 생성 결과를 메시지 편집기에 직접 연결한다. 수신자·연락처·현재 진료 연결은 기존 데이터에서 채우고 사용자가 확인한다. 긴 합성 문서에서 메시지를 수동으로 찾아 옮기게 하지 않는다.
- 기존 통합 문서와 새 구조화 결과를 구분한다. 확실히 분리되지 않은 기존 내용은 원문을 보존하고 사용자가 선택한 부분을 안내문으로 가져올 수 있게 한다.
- 환자용 미리보기에는 DB 필드명·관계 ID·스킬 지시·내부 검토 메모를 포함하지 않는다. 검토 상태는 편집 화면에서 별도로 보여준다.
- 처방 없음은 해당 없음으로 표시하고 가짜 복용법·기간 입력을 요구하지 않는다. 누락된 내용은 임의로 만들어 채우지 않는다.
- 생성한 초안의 저장·복사는 외부 전송과 분리한다. 현재 앱은 환자에게 메시지를 발송하지 않으며 기존 발송 상태는 수동/시험 기록임을 표시한다. 미동의·미승인도 로컬 초안 생성·저장이 가능하다.

### UX-07. 자동 저장·작업 진행·복구 — 2차

- 직원용 진료 편집에 자동 저장을 적용한다. 환자 설문의 정식 제출은 명시적 제출 행동으로 유지한다.
- `저장 중 / 저장됨 / 저장 실패 / 충돌`과 마지막 저장 시각을 표시한다. 응답을 확인하기 전 저장됨으로 표시하지 않는다.
- 입력을 잠시 멈추면 저장하고, 공간 이동 시 남은 입력을 보존한다. 미저장·실패 상태에서만 복구에 필요한 안내를 제공한다. 텍스트 초안은 로컬에서 관리하며 자동 저장으로 외부 서비스에 보내지 않는다. 사용자가 Cloud 생성을 누른 경우에만 선택 단계의 텍스트가 Apple PCC로 전달된다.
- 새로고침·연결 실패·서버 재시작 후 저장된 편집본을 복원하고, 남은 초안이 있으면 복구 여부를 선택한다. 마지막 저장 시점 이후 복구하지 못한 내용은 복구했다고 표시하지 않는다.
- 전사·의사 메모·SOAP·안내문을 서로 덮어쓰지 않게 저장한다. 같은 부분을 동시에 바꾼 경우에는 양쪽 내용을 보존해 비교·선택한다.
- AI 생성은 환자·세션·단계·입력 버전에 연결된 작업으로 표시한다. 생성 중 메모·원문 열람·다른 작업 공간 이동을 허용한다. 입력이 바뀌면 완료 결과를 이전 입력의 후보로 보여준다.
- 기존의 전체 화면 잠금과 전역 generating 상태만 제거해서 동시 작업을 구현하지 않는다. 저장 단위·입력 스냅샷·충돌 판정·결과 적용을 함께 변경한다.
- 생성 대기·진행·완료·실패를 구분하고 실행하지 않은 진행률은 표시하지 않는다. 재시도는 중복 결과 적용을 일으키지 않아야 한다. 긴 입력·모델 사용 불가·시간 초과에도 원문과 수정본을 보존한다.

### UX-08. MVP 동작과 AI 지침 일치 — 1차

- `clinic_ai/local_ai.py`는 원본 참조 문서의 미동의 초안 금지 지침을 로컬 MVP 정책으로 대체한다. 미동의·미승인도 로컬 후보 생성과 저장을 허용한다.
- 화면·서버·프롬프트·생성 후 메시지가 같은 원칙을 사용한다. 미동의는 로컬 시험 표시이며 생성 요청을 업무 규칙으로 중단시키지 않는다. 실제 외부 발송 기능을 추가하지 않는다.
- Notion 원본의 페이지 수정·블록 작성·DB 입력 안내가 제품 출력에 섞이지 않도록 로컬 출력 계약을 정한다. 참조 문서의 스킬 본문은 유지하며 개인정보·원본 링크는 공개본에서 제거한다.
- 동의 false를 미확정으로 바꾸거나 처방 없음에 설명서 작성을 요구하지 않는다. 결정적 UI 동작과 모델 문구를 나누어 검증한다.
- 임상 사실·수치·부정 표현에 관한 기존 AI 품질 한계는 별도로 기록하고 대조 검토를 지원한다.

### UX-09. 재진과 과거 기록 — 3차

- 환자와 개별 방문을 구분한다. 같은 환자의 방문 목록·이전 SOAP·처방/설명·검사·경과를 오늘 진료 옆에서 확인한다.
- 이전 기록은 작성 시점과 출처를 표시한다. 오늘의 진술·검사로 자동 복사하거나 AI에 무조건 모두 넣지 않는다.
- 이름만으로 환자를 자동 병합하지 않는다. 안정적인 로컬 환자 ID, 기존 세션 연결 기준, 동명이인 구분과 연결 정정 방법을 구현 전에 구체화한다.
- 기존 세션 ID와 문서·관계를 보존하는 마이그레이션, 백업 복구, 연결 실패 기록 처리를 포함한다. 미연결 과거 기록도 열람 가능해야 한다.
- 환자 모델 확장은 승인된 제품 범위다. 아래의 보조 테이블로 기존 7개 업무 테이블과 행 ID를 보존한다.

### UX-10. 시각·입력·상태 일관성 — 1차, 이후 회귀 유지

- 핵심 작업 버튼과 보조 작업을 시각적으로 구분한다. 반복되는 큰 안내 배너를 줄이고 필요한 주의점은 해당 위치에 표시한다. MVP·로컬 상태는 간결하게 상시 확인할 수 있어야 한다.
- 기본 데스크톱은 1280×720에서 환자 식별·작업 탭·대표 행동을 첫 화면에서 확인한다. 좁은 화면은 참고 패널을 접고 작업 공간을 우선한다.
- 설문은 390px 폭에서도 가로 스크롤 없이 입력한다. 데스크톱 키보드 탐색, 명확한 포커스, 필드 레이블과 오류 연결을 유지한다.
- 저장·마이크·생성 상태를 색상만으로 표현하지 않는다. 안내·실패 메시지는 관련 행동 가까이에서 읽고 다시 확인할 수 있어야 한다.

## 6. 단계별 구현 범위와 완료 조건

| 단계 | 범위 | 완료 조건 |
| --- | --- | --- |
| 1차: 환자 중심 화면 | UX-01~03, UX-04 배치, UX-05 표시, UX-06, UX-08, UX-10 | 진료 목록→환자 작업 화면→검사·기록·안내를 연결한다. 메시지 수동 전사와 환자 재선택을 없애고 MVP 지침을 일치시킨다. |
| 2차: 작업 연속성 | UX-04 동시 메모, UX-05 비교·복구, UX-07 | 전사 중 메모, 생성 중 다른 작업, 자동 저장, 후보 비교·적용, 실패 복구를 실제로 검증한다. |
| 3차: 환자별 경과 | UX-09와 기존 기능 회귀 | 환자별 여러 방문을 정확히 연결하고 이관·연결 정정·복구를 검증한다. |

각 단계에서 기존 데이터와 미완성 단계의 기능을 보존한다. 1차의 화면 개편만으로 자동 저장·생성 중 작업까지 완료했다고 보고하지 않는다. 기존 API·로컬 엔진을 재사용하고 단계에 필요한 최소 확장을 한다.

## 7. 수용 기준과 검증 시나리오

아래 기준은 구현 후 확인할 항목이며 현재 통과 상태가 아니다. 기존 합성 사례(`.build/qa-case/source.json`, `transcript.txt`, `synthetic.aiff`)를 입력으로 사용하고 변경을 일으키는 시험은 임시 DB에서 수행한다. 운영 시험 DB를 초기화하거나 기존 진료 자료를 클라우드에 보내지 않는다. Cloud 비교 시험에는 스크립트에 명시된 가상 자료만 사용한다.

| ID / 단계 | 시나리오 | 기대 결과 |
| --- | --- | --- |
| AC-01 / 1차 | 오늘 목록에서 환자를 열어 검사·문진·기록 검토·안내 준비 | 정상 흐름의 별도 DB 메뉴 왕복 0회, 같은 환자 재선택 0회. 작업 탭 왕복에도 같은 방문 유지 |
| AC-02 / 1차 | 1280×720 첫 화면 및 상태 필터 확인 | 환자·주소증·작업 탭·대표 행동 표시. 목록의 필터와 바로가기 결과 일치 |
| AC-03 / 1차 | 열린 진료에서 혈압·맥박·체온 입력 | 세션 자동 연결, 단위·미입력 구분, 저장 후 요약 갱신 |
| AC-04 / 1차 | 설문의 복용약 없음/있음·형식 오류·중복 제출 | 의미 보존, 상세 입력 조건 일치, 답변 유지, 설문/세션 중복 생성 없음 |
| AC-05 / 1차 | SOAP와 설문·검사·전사 대조 | 작업 맥락을 유지하며 근거 열람, S/O/A/P 편집 및 편집본 복사 가능 |
| AC-06 / 1차 | 안내 생성→메시지 편집→로컬 저장·복사 | 메시지 수동 옮겨 적기 0회, 수신자 연결, 환자용 결과에 내부 지시/DB 필드 없음, 환자 메시지 발송 없음 |
| AC-07 / 1차 | 미동의·미승인·발송일 공란 및 처방 없음 | 업무 상태로 로컬 생성·저장 차단 없음, 표시와 프롬프트 일치. 모델 오류는 업무 게이트와 구분 |
| AC-08 / 2차 | 실제 마이크 전사 중 메모·진찰 소견 입력·작업 탭 이동 | 입력 가능, 각각 저장, 같은 세션 귀속, 음성 파일 생성 0. 정지/취소/권한 거절/연결 종료 시 마이크 정리 |
| AC-09 / 2차 | 편집 후 탭 이동·새로고침·저장 실패·서버 재시작 | 저장 확인된 내용 보존, 실패 상태 표시, 남은 초안 복구, 미저장 내용을 저장됨으로 오표시하지 않음 |
| AC-10 / 2차 | AI 생성 중 다른 메모 편집·원자료 변경·화면 이동 | UI 전체 잠금 없음, 입력 버전 연결, 변경 후 결과는 후보로 남고 최신 편집본을 덮어쓰지 않음 |
| AC-11 / 2차 | 수정한 SOAP 재생성·비교·부분 적용·이전 버전 복구 | 사용자 수정본 보존, 선택한 부분만 적용, 복사/내보내기에 적용본 반영 |
| AC-12 / 각 단계 | 파일 업로드·텍스트 입력·오류·기존 전사 이력 확인 | 기존 입력 기능·원문·시간·이력 보존. 파일 전사 임시 파일 제거, 마이크 오디오 미생성 |
| AC-13 / 3차 | 재진·동명이인·기존 세션 이관·연결 정정·백업 복구 | 잘못된 자동 병합 없음, 방문별 기록 보존, 이전 검사와 오늘 검사 구분, 기존 링크 유지 |
| AC-14 / 각 단계 | 키보드·좁은 화면·오류/저장/마이크 상태 확인 | 레이블·포커스·텍스트 상태 제공. 설문 390px 가로 스크롤 없음, 직원 화면의 참고 패널 접기 가능 |
| AC-15 / 각 단계 | 기존 DB 사본 업그레이드·빈 DB 시작·기존 링크 열기 | 기존 행 ID·본문·관계·이력 보존, integrity_check=ok, 빈 DB에 샘플 자동 생성 없음 |

같은 사례로 작업 시간, DB 메뉴 이동 횟수, 환자 재선택 횟수, 메시지 옮겨 적기 횟수, 저장 손실/충돌을 기록한다. 개선율은 측정 후 보고한다. 실제 생성의 부정문·수치 오류도 별도로 기록하며 기능 통과와 임상 품질을 합치지 않는다.

## 8. 데이터·AI·음성의 기술 기준

### 데이터와 원천

원본 Notion 주소와 계정 식별자는 공개본에서 제거했다. `clinic_ai/source_schema.json`은 7개 DB 구조, `clinic_ai/source_docs.json`은 화면 열람과 AI에 사용하는 9개 참조 문서다. 2026-09-17 확보한 스냅샷이며 자동 동기화하지 않는다.

| 기존 테이블 | 역할 |
| --- | --- |
| surveys | 초진설문·동의·인적사항 |
| exams | 신체검사·세션 연결 |
| sessions | 진료 흐름·전사·가/나/다·SOAP |
| knowledge | 한의학 참고 자료 |
| crm | 환자 안내 메시지·수동 처리 기록 |
| templates | 안내 템플릿 |
| settings | 운영 설정·점검 |

현재 행은 속성/본문 JSON, 생성·수정 시각과 revision을 저장한다. 관계는 로컬 ID로 연결하고 역방향은 계산한다. 설문 제출과 세션 연결은 한 트랜잭션으로 수행한다. 담당자는 로컬 이름 텍스트다. 나이·성별 계산은 원본 스킬 기준이며 Notion formula와 완전 동일함을 보장하지 않는다.

### 확정한 환자·방문 및 작업 저장 구조

- `ux_patients`: 무작위 로컬 환자 ID와 표시 이름. 이름만으로 자동 병합하지 않는다.
- `ux_visits`: 기존 세션 ID와 환자 ID의 연결. 초기 이관은 기존 세션마다 별도 환자를 만든다. 동명이인은 짧은 환자 ID와 방문 수로 구분한다.
- `ux_links`: 사용자가 선택한 방문의 환자 연결 정정 이력. 기존/새 환자 ID·시각을 보존한다. 이전 환자로 다시 연결해 정정할 수 있다.
- `ux_versions`: 항목 편집 전 문서 스냅샷. 선택 항목 복구 시 다른 항목을 보존하고 복구 전 버전도 남긴다.
- `ux_jobs`: 단계별 입력 스냅샷·fingerprint·상태·후보·오류. 원자료 변경은 후보/적용 결과의 이전 입력 표시로 이어지며 자동 덮어쓰지 않는다.
- 새 방문은 인적사항만 가져온 별도 설문·세션을 만든다. 증상·약·검사는 오늘 확인 대상으로 남기며 과거 SOAP와 처방은 참고 패널에서 읽는다.
- 최초 확장 전에 같은 폴더에 `*-before-ux-*.sqlite3` 백업을 만든다(빈 DB 제외). 기존 행을 수정하지 않고 schema version 3으로 확장한다. 기존 version 2에서 모델 메타데이터 열을 추가할 때는 `*-before-models-*.sqlite3` 백업을 만든다. 서버 종료 후 전체 SQLite 백업을 복원하면 환자 연결·후보·이력까지 함께 복구된다.
- 항목별 자동 저장은 각 항목의 원래 값과 비교한다. 다른 항목 변경은 병합하고 같은 항목 변경은 양쪽 값을 보여준다. 저장 실패 시 텍스트 초안은 같은 브라우저 origin의 localStorage에 보존하고 복구/버리기를 선택한다. 환자 설문은 명시적으로 제출한다.

운영 읽기·쓰기 정본은 실행 시 지정한 로컬 SQLite다. 자동 저장·후보 결과·이력 확장은 같은 로컬 저장 경계 안에서 설계한다. DB·전사 원문·음성·QA 자료·자격증명·빌드 결과는 Git에서 제외한다.

### AI 모델 선택과 출력 계약

`FoundationBridge.swift`가 `SystemLanguageModel.default`를 호출한다. 이전에 이 Mac에서 확인한 값은 AFM 3 Core Advanced, 사용 가능, contextSize 8192다. 실제 가용성은 상태 API로 확인한다. 이름·연락처·주민번호 속성은 현재 AI 입력 대상에서 제외하며 자유 입력에 포함된 식별정보는 자동 제거되지 않는다.

단계별 입력은 해당 설문·검사·선택 자료·전사 또는 SOAP·설명/처방이다. 사용자 메모와 입력 버전을 명시적으로 구분한다. 생성 결과는 후보로 남고 선택한 항목만 편집본에 적용한다. 임상 규칙은 참조 문서에서 유지하되 Notion 실행·배치 규칙은 로컬 출력 계약으로 대체한다.

문맥 초과·모델 거부·사용 불가·시간 초과 시 기존 입력/결과를 보존한다. 긴 전사 자동 분할과 실패 후 자동 모델 전환은 이번 범위에 포함하지 않는다. 입력을 줄여 다시 시도할 수 있게 안내하되 원문 자체를 잘라 덮어쓰지 않는다.

2026-09-17 승인된 확장:

- 목표: 기존 가/나/다 스킬·후보 검토·부분 적용 흐름에서 `로컬 / Cloud / Cloud Pro`를 명시적으로 선택한다.
- 범위: `local_ai.py`의 공통 프롬프트, `ai_models.py`의 Shortcuts 호출, 작업 큐 메타데이터, 화면 선택·처리 위치, 두 전용 단축어.
- 완료 조건: 세 모델의 실제 파일 입출력, 완전한 결과 구조, 모델·프롬프트 식별, 실패 시 기존 문서 보존, 인앱 사용 흐름을 검증한다.
- 제외 범위: 기존 진료 데이터의 시험용 클라우드 전송, 자동 fallback·자동 재시도, PCC 직접 entitlement, 환자 자동 발송, 마이크/파일 전사 경로 변경.
- 검증: 합성 자료의 동일 입력으로 세 모델 가/나/다를 비교하고, 정답 전사와 Apple ASR 전사를 별도로 비교한다. JSON/구조 통과와 임상 내용 정확성을 분리한다.

기본 선택은 로컬이다. 각 진료 화면에서 Cloud를 선택하면 생성 시 설문·검사·참고자료·메모와 전사 또는 SOAP·설명/처방 텍스트를 Apple Private Cloud Compute로 보낸다고 표시한다. 이름·연락처·주민번호 속성을 입력에서 제외하더라도 자유 입력의 식별정보는 자동 제거되지 않는다. 모델 선택만 바꾸거나 저장할 때는 생성하지 않는다. 오디오 전사는 계속 이 Mac에서 처리한다.

`AFM Cloud 호출`과 `AFM Cloud Pro 호출`은 `단축어 입력 텍스트 → 모델 사용(Cloud 출력=사전 / Cloud Pro 출력=텍스트) → 응답 출력`으로 구성한다. `python3 -m scripts.prepare_shortcuts --sign`으로 입력 변수와 모델 구성을 담은 파일을 준비하고 단축어 앱에서 가져온 뒤 `python3 -m scripts.prepare_shortcuts --connect`로 검증·연결한다. 연결 정보는 Git 제외 `data/ai-shortcuts.json`에 저장한다. 이 명령은 macOS 27에서 확인한 단축어 DB 구조를 읽기 전용으로 검사하며 DB에 쓰지 않는다. Apple의 앱 DB 구조가 바뀌면 연결 검증을 갱신해야 한다.

실행은 `/usr/bin/shortcuts run <고정 UUID> --input-path … --output-path … --output-type …`다. Cloud는 사전 출력이므로 `public.json`, Cloud Pro는 텍스트 출력이므로 `public.plain-text`로 내보낸다. 사전 출력도 요청 ID·필수 키·문자열 타입 검사를 통과해야 후보로 저장한다. 호출 직전 동작 해시를 확인해 모델 설정이 바뀌었으면 재연결을 안내한다. 임시 텍스트는 디렉터리 0700/파일 0600으로 만들고 성공·실패·시간 초과 후 제거한다. 별도의 오디오를 생성하거나 보내지 않는다. 모델 반환 텍스트를 표준 출력/로그에 기록하지 않는다.

Cloud 출력은 JSON 객체와 요청마다 다른 `request_id`를 요구한다. 단축어의 서식 처리에서 키의 밑줄을 보존하도록 JSON 코드 펜스로 감싼 텍스트를 요청한다. 코드 펜스는 전체 응답을 감싼 경우에만 제거한다. 문자열 내부의 실제 줄바꿈 같은 잘못된 JSON은 임의로 고치지 않는다. 잘못된 JSON·중복 키·다른 요청 ID·빈 필수 값은 후보 실패로 처리한다. 가는 `content`, 나는 `content`와 네 SOAP 필드, 다는 `content/guide/rx_guide/message`를 검사한다. Swift 결과도 같은 완전성 검사를 거친다. 파싱 성공은 사실 정확성 보장이 아니다.

큐에는 CLI로 전달할 완성 텍스트와 요청 ID를 포함하여 모델 ID, 처리 위치, 단축어 UUID·동작 해시, 프롬프트/출력 계약 버전과 실제 요청 스냅샷을 고정한다. 후보/적용 출처에 같은 정보를 남긴다. Cloud의 표시 모델은 단축어 설정으로 확인한 이름이며 모델 응답의 자기소개를 증거로 쓰지 않는다. 실패 재시도는 현재 선택 상자와 무관하게 원래 모델로 새 입력 스냅샷을 만든다. 자동 재요청은 없고 시간 초과 때 원격 실행 종료 여부는 미확인이라고 표시한다.

| 확장 기준 | 수용 시나리오 |
| --- | --- |
| AI-01 | 실제 두 단축어가 이번 파일 입력의 요청 ID와 올바른 JSON 결과를 반환한다. |
| AI-02 | 모델 선택만으로 요청하지 않으며 생성 때 선택 모델/위치를 표시하고 큐·결과·적용 출처에 보존한다. |
| AI-03 | JSON·SOAP·안내 필드 실패/시간 초과/모델 설정 변경 시 기존 문서 보존, 자동 fallback 0회. |
| AI-04 | 로컬 기존 동작, 미동의 후보 생성, 부분 적용, 파일·마이크 전사, 기존 DB·빈 DB 보존. |
| AI-05 | 세 모델 동일 입력 및 정답/실제 ASR 입력 비교, 임상 오류와 입출력 기능 결과를 별도 기록. |

### 음성 전사

전사는 Foundation Models와 별개인 SpeechAnalyzer + SpeechTranscriber(ko_KR)를 사용한다. 시간 정보와 제공되는 신뢰도를 보존하되 신뢰도를 정확도 점수로 표시하지 않는다. 화자·의사/환자 역할 자동 분리는 이번 범위에 포함하지 않는다.

- 파일: WAV/M4A/MP3/AIFF/AIF/CAF, 최대 256MB·2시간. 권한 0600 임시 파일로 처리하고 성공·실패 후 제거한다. 기존 사용자의 원본 파일은 삭제하지 않는다.
- 마이크: AudioWorklet 16kHz mono float32 PCM을 약 200ms 단위로 localhost→Python pipe→Swift 메모리 버퍼로 전달한다. 중간 결과·확정 결과를 구분한다.
- 정지·취소 시 track과 프로세스를 정리한다. 연결 중단 30초 후 작업 제거, 대기열 한도·입력 순서 검사를 유지한다.
- 전사 본문 한도는 100,000자다. 실제 임상 용어·겹침·긴 진료 성능은 별도 평가 대상으로 남는다.

## 9. 실행과 검증 명령

프로젝트 루트는 `저장소 루트`다. 현재 주소는 `http://127.0.0.1:8766/`, 환자 설문은 `/survey`다. 서버는 `clinic_ai.app` 하나만 사용한다.

```sh
# 현재 시험 세션 사용
python3 -m clinic_ai.app --db .build/qa-case/clinic.sqlite3 --port 8766

# 기본 빈 DB를 사용하려는 경우에만 위 서버 대신 실행
python3 -m clinic_ai.app --db data/clinic.sqlite3 --port 8766
```

`--db` 생략 시 `data/clinic.sqlite3`를 사용한다. 현재 시험 DB를 재개하려면 `.build/qa-case/clinic.sqlite3`를 명시한다. 같은 포트에 서버를 중복 실행하지 않는다. 새 DB에는 샘플 행을 자동으로 넣지 않는다. `--reset`은 백업 후 지정 DB를 비우는 명시적 초기화이므로 UX 개편에 사용하지 않는다.

현재 빌드 환경은 Apple Silicon, CLT Swift 6.4 / macOS 27 SDK다. Python 서버는 표준 라이브러리를 사용한다.

```sh
sh scripts/build_foundation.sh
sh scripts/build_speech.sh
sh scripts/build_live_speech.sh
python3 -m clinic_ai.app --db .build/qa-case/clinic.sqlite3 --counts
python3 -m unittest discover -s tests -v
# DOM 시험 도구 최초 준비 (Node 필요)
npm install --prefix .build/qa jsdom@30.0.1
python3 -m scripts.smoke_ui
# 기존 합성 파일을 메모리로 읽는 실제 스트리밍 시험 (ffmpeg 필요)
python3 -m scripts.smoke_live_speech .build/qa-case/synthetic.aiff
# 실제 파일 전사 및 AFM 시험: 별도 임시 합성 오디오 생성
python3 -m scripts.smoke_speech
python3 -m scripts.smoke_local_ai
# 새 작업 큐/부분 적용/구조화 안내의 실제 AFM 시험
python3 -m scripts.smoke_ux_ai
# 명시적으로 Cloud에 합성 텍스트를 보내는 세 모델 비교
python3 -m scripts.smoke_model_comparison
```

기존 테스트는 회귀 기준이다. 개편하면서 변경된 UI에 맞춰 시나리오와 선택자를 갱신하되 데이터 보존·MVP 비차단·실제 기능 검증을 제거하지 않는다. 단위/HTTP/DOM 시험과 실제 엔진 시험을 구분하고 실제 인앱 배치·권한·입력 흐름은 별도로 확인한다. 제품 검증 결과는 실행 환경과 함께 `history.md`에 기록한다.

## 실시간 전사 UX 확장 — 2026-09-17

- 목표: 말하는 동안 입력 여부와 인식 진행을 확인할 수 있는 한 개의 전사 본문.
- 범위: 마이크/문진/SOAP 웹 화면, 음량 피드백, 기존 저장·복구 흐름과 검증.
- 완료 조건: UX-L1 입력 음량은 서버 응답과 독립적으로 갱신, UX-L2 회색 임시 문장 교체→검은 확정 문장, UX-L3 전사/SOAP는 읽기 기본·항목별 편집, UX-L4 종료/취소/오류/탭 이동 및 파일 전사 보존.
- 제외: 자동 LLM 교정, 자동 화자 지정, 임상 정확성 보장, 마이크 오디오 파일 생성, 실제 진료 자료 외부 전송.
- 검증: DOM 상태 전환·편집/저장/복구 회귀, 실제 합성 PCM 엔진 지연 측정, 승인된 인앱 배치/마이크 확인. 서버 전송 간격·엔진 최초 응답·화면 표시 지연을 구분한다.

전사 본문은 기존 저장 문장과 이번 확정 문장을 검은색으로, 현재 인식 중인 끝부분을 회색으로 표시한다. 검은색은 음성 인식 확정이며 임상 검토나 LLM 교정 완료를 의미하지 않는다. 이번 전사는 종료 때 저장한다. 실시간 엔진에 `.volatileResults + .fastResults`를 사용한다. Apple이 명시한 빠른 응답/정확도 절충이 있으며 파일 전사 설정은 바꾸지 않는다. 입력 음량은 Web Audio 분석기로 직접 표시하며 첫 글자가 아직 없으면 듣는 상태를 표시한다. 실제 음량에만 파형이 반응한다. 텍스트 수신은 활성 마이크 동안만 조회하고 결과 노드만 바꿔 선택·포커스를 유지한다.

마이크 주요 조작은 하단 한 곳에 둔다. 파일/텍스트 가져오기와 폐기는 보조 메뉴로 옮긴다. 기존 전사와 실시간 전사를 한 본문으로 통합하고 전사 수정은 마이크 종료 후 가능하다. 의사 메모는 접어서 제공하며 마이크와 동시에 편집할 수 있다. SOAP는 읽기 기본이며 필요한 항목만 편집한다. 원본·시간·이력·충돌 복구는 유지한다.

## 10. 원샷 실행 프롬프트 — v12 (SOAP 분할 실험 결과 반영)

```text
저장소 루트에서 Agent.md와 이 PRD를 읽고 UX-01~10, AC-01~15, AI-01~05를 보존하고 UX-L1~L4 및 네이티브 N1~N7을 충족하라.
history.md N4~N5와 최신 Q2~Q5를 함께 읽어라. 과거 Cloud 성공을 현재 빌드의 성공으로 대체하지 마라. Q5 후속에서 사용자 승인으로 전체 디스크 접근을 켰고 SQLITE_CANTOPEN은 재현되지 않았다. 최근 Cloud Pro 실행 실패는 사용 한도 도달로 확인됐다. S1에서 개발자 서명으로 전환하고 다른 해시의 빌드에서도 권한 유지를 검증했다. 추가 설정 변경은 별도 승인을 받은 뒤 수행하라. CLI 검증을 앱 runtime 검증으로 대체하지 마라. 남은 로컬 긴 전사 생성 실패와 VoiceOver/축소 창 업무·설치 검증을 구분하여 진행하라. 권한 확대나 검증 제거로 우회하지 마라.
기존 변경·7개 업무 테이블·행 ID·본문·관계·전사 이력을 보존하라. 변경 전 SQLite 백업을 만들고 시험은 임시 DB 또는 명시한 시험 사본에서 하라.
환자 작업 화면, 항목별 자동 저장/충돌 복구, 백그라운드 AI 후보/부분 적용/버전 복원, 환자 ID와 방문 연결을 함께 검증하라.
MVP 동의·승인 상태로 로컬 작업을 차단하지 마라. Apple 로컬 API, 파일 전사, 마이크 오디오 파일 미생성을 유지하라.
각 회차에 실행 → 결과 관측 → AC별 평가 → 실패 원인을 반영한 프롬프트 보완을 수행하고 history.md에 명령·변경·실패·재시험 결과를 남겨라.
지연 응답·같은 항목 충돌·다른 항목 병합·빠른 탐색·재로딩·서버 재시작·빈 항목 복구·입력 변경 후 AI 후보·환자 연결 정정·전체 백업 복구를 검증하라.
단위/HTTP/DOM 결과와 실제 엔진/인앱 GUI 증거를 구분하라. 실제 GUI는 기존 승인 범위에서 1280×720 직원 화면, 390px 설문, 마이크 동시 메모/탭 이동/정지/취소를 확인하라.
최종 코드로 집중·전체 회귀를 마친 뒤 지정 runtime을 재시작하고 기존 DB와 빈 DB를 대조하라. 기존 진료 자료를 Cloud 시험에 보내지 마라. 명시된 합성 자료로만 Cloud를 시험하며 자동 모델 fallback·원격 게시·환자 발송은 하지 마라.
AC의 일부 증거나 과거 코드의 통과로 완료를 선언하지 마라. 실패를 해결한 뒤 같은 시나리오를 다시 시험하고 15개 기준에 각각 증거를 연결하라.
원문의 부정 표현·수치·미검사를 실제 생성 결과와 대조하라. 모델 오류는 숨기지 말고 기록하라. 유한한 수용 시나리오의 통과를 모든 임상 출력의 100% 정확성으로 확대하지 마라.
SOAP 분할은 시험 메뉴에서만 제공하고 자동 기본 승격하지 마라. loop 4회/39회 CLI 시도의 임상 품질 미달을 보존하라. 원문·질문/답 묶음·항목별 지침을 고정하고 5회 호출 결과를 코드로 조립하라. 분할이 더 좋다고 가정하지 말고 같은 입력의 단일 생성과 대조하라. 긴 무구조 문단/묶음 간 최신 사실 통합은 아직 해결되지 않았다. JSON 키 보존과 본문 지시를 구분하라. 출력 계약을 명시하고 큐에 최종 CLI 입력을 고정하라. Cloud 사전/public.json, Cloud Pro 텍스트/public.plain-text의 실제 반환 형식을 구분하라. 공통 프롬프트와 입력을 고정해 세 모델을 비교하라. JSON 검증, 단축어 출력 형식, 임시 파일 정리, 재시도 모델 고정, 적용 출처를 검사하라.
마이크 피드백은 서버 응답과 분리하라. 회색 임시 구간을 교체하고 확정 시 검은색으로 이동하되 저장 문장을 중복하지 마라. 종료·취소·복구·다른 환자 탐색·메모 동시 입력·편집 포커스를 검증하라. 실제 엔진 지연과 UI 시험의 모의 응답을 구분하라.
완료 보고에는 실제 통과 범위, 변경 문서 경로, 데이터 보존, runtime과 Git 상태, 남은 임상 품질 한계를 구분하라.
```


## SOAP 모델 품질 비교 — 2026-09-17

- 목표: AFM 3 네 종류의 접근 가능 여부와 SOAP 원문 충실도를 분리해 평가한다.
- 범위: 정확한 합성 대본과 실제 Apple ASR 입력, 기존 프롬프트와 충실도 보강 후보, 명시적 모델 선택. 실제 진료 DB 변경·실제 환자 자료 전송 없음.
- 완료 조건: 각 모델의 실측 실행 상태·반복 결과·누락/의미 확대/수치/미검사/처방 미정·불확실성 보존 판정과 근거를 남긴다. 호출할 수 없는 모델은 미시험으로 표시한다.
- 비교: baseline 1회 및 fidelity-v1 2회 × gold/ASR × 접근 가능한 각 모델. Swift 구조화 출력과 Shortcuts JSON 계약이라는 transport 차이를 명시한다. 보강 프롬프트는 시험용이며 앱 기본값을 자동으로 바꾸지 않는다.
- 검증: scripts/evaluate_soap_quality.py, .build/soap-quality 산출물, 사람 독해에 준하는 원문/출력 대조 기록. 문자열 검사나 형식 통과만으로 내용 합격을 부여하지 않는다. Core는 공개 SDK에서 선택 가능 여부가 확인되지 않아 Core Advanced 중복 실행으로 대체하지 않는다.

## SOAP 분할 생성 실험 — 승인된 실행 계획 (2026-09-17)

- 목표: 한 번의 생성에 집중된 사실 보존·분류·서술을 나누어 SOAP 충실도를 개선한다.
- 범위: Python 생성/큐 계약, 기존 Swift·Shortcuts 호출, 합성 자료 실험, PRD/history 갱신. 모델은 사용자가 선택한 하나로 고정한다.
- 완료 조건: 원문을 보존하는 분할 생성과 코드 조립, 실패 시 기존 문서 보존, 큐 입력 고정, 동일 합성 입력의 단일/분할 비교 및 독해 평가. 품질 합격은 정확한 대본의 부정·수치·한정어·미검사·미정 보존과 ASR 모호성 표시를 모두 요구한다. 미달이면 미달을 보고하며 기준을 낮춰 합격시키지 않는다.
- 제외: 실제 진료 자료 Cloud 전송, 자동 모델 전환, 후보 자동 적용, 임상 정확성 보장, Core 중복 집계.
- 검증: 각 회차를 계획→구현→실험→회고→PRD 갱신으로 센다. 형식/회귀 시험과 실제 모델 품질을 구분하고, 품질이 개선되지 않는 모델에는 무한 지침 추가 대신 한계를 기록한다. 최종 보고는 실제 회차 수와 최선이라는 판단의 범위를 포함한다.

분할 방식은 원문 묶음별 사실 추출 후 해당 원문과 추출 결과를 함께 제공해 S/O/A/P를 따로 작성한다. 최종 본문은 코드로 결합한다. 질문과 답을 같은 묶음으로 유지하고, 분리 근거가 없는 과대 문단은 임의 절단하지 않는다. 이는 전체 문서의 입력 길이를 각 호출에 제한하기 위한 방식이며 모델 능력 향상을 보장하지 않는다. 중간 산출물·호출 시간·가능한 토큰 수는 후보에 남긴다. 외부 모델 토큰 수는 미제공으로 표시한다.

분할 실험 1회차 반영: 사실 추출은 자유 본문 제목 파싱을 사용하지 않는다. 기존 subjective/objective/assessment/plan 구조화 출력 계약을 재사용한다. 추출과 각 항목의 지침도 큐 생성 시 고정한다. 구조화 실패는 부분 문서를 적용하지 않고 기존 기록을 유지한다.

분할 실험 2회차 반영: 각 항목 작성에는 해당 항목 추출 메모와 원문을 함께 제공한다. content 필드의 설명도 S/O/A/P별로 맞춘다. 기본 생성은 유지하고 네이티브 AI 초안 메뉴에 'SOAP 분할 생성 · 시험'을 제공해 비교 가능하게 한다. 재시도는 모델과 생성 방식을 유지한다. 중간 호출의 실제 입력은 선행 출력에 의존하므로 큐에는 원문·분할·단계 지침을 고정하고 실행 시 요청 해시와 중간 결과를 보존한다. ASR 의미 추정 문제가 남아 있으므로 품질 검증 없이 기본값으로 승격하지 않는다.

분할 실험 3회차 반영: 로컬/Cloud 모두 content 필드 설명을 실제 항목과 일치시킨다. 구두점만 반환한 결과는 본문으로 채택하지 않는다. 분할은 품질이 검증된 기본 경로가 아니며 시험 메뉴에 유지한다. 호출 성공과 임상 품질 합격은 별도 집계한다. holdout에도 부정·알레르기·검사 시점·미검사·처방 결정 상태 보존 및 S/O/A/P 분류를 요구한다.


### 분할 실험 최종 판정 — loop 4

- 구현: `soap-split-v4`, 네이티브 **AI 초안 메뉴 → SOAP 분할 생성 · 시험**, API `soap_strategy: split`. 기존 단일 생성이 기본이다. 실패/재시도에 선택 모델과 방식을 보존한다.
- 검증: Python 77개, Release 빌드, 실제 네이티브 로컬 생성/부분 적용/기존 편집 보존 통과. 품질은 Core Advanced/Cloud 미달, Cloud Pro도 실제 ASR 불확실성 처리 미달. Core는 여전히 별도 호출 미확인.
- 원문은 매 작성 호출에 제공하고, 입력 자료를 임의 절단하지 않는다. 명확한 의사 문답 경계로 묶으며 한 묶음 2,400자 초과 시 원문 보존과 문단 정리를 안내한다. 이 값은 토큰 수 보장이 아니며 실제 Foundation Models 토큰 검사는 유지한다. 무구조 장문 자동 분할·묶음 간 상충/최신 사실 통합·출처 인용의 자동 의미 검증은 미완료다.
- 한 묶음당 5회 호출이며 로컬의 호출당 입력은 줄었어도 총 입력량은 늘었다. Cloud Pro의 최종 성공 시나리오 평균 11.03초는 전체 품질 합격이나 최적성의 증거가 아니다. 최근 단일 재비교는 transport 실패라 Q1의 역사적 결과와 구분한다.
- 미완료: Cloud Pro 사용 한도 해제 후 생성 재시험, ASR 모호성 처리. 전체 디스크 접근 on과 SQLITE_CANTOPEN 미재현은 확인했다. 권한 확대/검사 제거/자동 fallback으로 우회하지 않는다. 100% 정확성 또는 최선 달성으로 표시하지 않는다.


### 안정된 개발 서명 — 2026-09-17

- 목표: 코드가 바뀌어도 같은 앱으로 식별되어 기존 개인정보 권한이 이어지도록 한다.
- 범위: CLI/Xcode 서명 설정, 현재 유효한 Apple Development 인증서와 로컬 설정의 개발 팀, Bundle ID com.raphael.ClinicAI 유지.
- 완료 조건: 두 빌드의 designated requirement 일치, 내용/해시가 달라진 재빌드 후 권한 on 유지 및 실제 앱 경로 접근 확인.
- 제외: 배포용 Developer ID 발급, 공증/배포, TCC DB 직접 수정, 타 앱 권한 변경.
- 검증: codesign 검증, Xcode Release 빌드, System Settings 확인, 앱 소유 서버의 합성 Cloud 재시험. 개발 인증서가 없거나 만료되면 빌드를 실패시키며 ad-hoc 서명으로 자동 전환하지 않는다.

안정된 서명 검증 결과(S1): CLI/Xcode Release의 CDHash가 서로 달라도 designated requirement가 같음을 확인했고, 새 서명에 최초 권한을 연결한 뒤 두 번째 빌드에서 재허용 없이 단축어 DB 접근과 권한 on이 유지됐다. 인증서 만료 또는 갱신 시 CLI/Xcode 설정을 함께 검토한다. 현재 Cloud Pro의 실행 실패는 Apple 사용 한도 도달이며, 제품은 이를 별도 안내(429)로 표시한다. 한도 해제 시점은 제공되지 않아 추정하지 않는다.


### Cloud 사용 한도 관측 및 운영 기준 (S2, 2026-09-17)

- 10:46 KST 합성 CLI 비교: Cloud 응답 성공, Cloud Pro 사용 한도 오류. Pro 제한이 두 모델을 함께 차단하지는 않았지만 quota 완전 독립은 미확인이다. Cloud 성공을 SOAP 임상 품질 통과나 앱 runtime 재검증으로 간주하지 않는다.
- Apple [한도 안내](https://support.apple.com/ko-kr/127901)는 기능·요청 복잡성·수요·정책에 따라 제한이 달라질 수 있다고 설명한다. [macOS 27 공식 가이드 각주](https://support.apple.com/en-by/guide/notes/not9474646a9/mac)에는 일일 한도가 명시돼 있다. 숫자·잔여량·초기화 시각·모델 간 공유 범위는 확인되지 않았다. 이번 오류의 제한 기간도 추정하지 않는다.
- 사용자가 모델을 직접 선택하며 한도 도달 후 자동 전환·반복 호출하지 않는다. 5단계 분할은 호출 수를 늘리지만 실제 quota 차감 단위가 호출 수인지 토큰/연산량인지는 미확인이다.

### 동일 음원 STT 비교 결과 (STT-1, 2026-09-17)

- 85.67초 합성 WAV를 실제 진료실 앱과 Notion AI 노트에 각각 업로드했다. 이 단일 사례에서는 Notion이 일부 용어 및 대본 문자 일치에서 소폭 우세였으나 둘 다 소변/압통/심부건반사/요통 오인식이 남았다. 상세 기준·원시 결과·한계는 history.md STT-1에 기록한다.
- 파일 전사 성공을 진료 전문용어 정확성, 화자 분리, 실시간 표시 지연, 실진료 품질 합격으로 대신하지 않는다. 현재 결과만으로 STT 엔진 교체나 자동 LLM 교정을 기본값으로 승격하지 않는다.
- 후속 STT-2는 사용자 승인에 따라 두 합성 화자의 같은 음원을 가상 마이크로 실시간 입력해 비교한다. 파일 업로드와 구분하며, 첫 임시 글자/확정 표시 지연 및 화자 ID 일관성을 관측한다. 관측 간격과 오디오 라우팅 조건을 함께 기록한다. 물리 마이크·소음·중첩 발화 성능은 이 시험으로 검증되지 않는다. BlackHole 설치는 관리자 인증 대기에서 사용자 지시로 중단했으며 설치 완료와 비교 결과는 미확인이다. 사용자 재개 요청 후 이어간다.

### 공개 저장소 조건 — 2026-09-17

- 목표: 소스·문서·회귀 테스트를 개인정보 없이 공개한다.
- 범위: 추적 파일, Git 이력·작성자, GitHub 공개 범위, 개발 서명 설정.
- 완료 조건: 공개 이력에 개인 이메일·홈 경로·원본 Notion 식별자·서명 식별값·진료 데이터가 없고, 정리 후 회귀·빌드 검증을 통과한다.
- 제외: 기존 진료 DB·음원·실행 중 앱·macOS 권한은 변경하지 않는다.
- 검증: 비밀정보 탐지, 식별자·파일 목록 감사, 전체 회귀, Xcode 빌드·서명, 원격 재복제 검사.
- 기존 이력은 비공개 보관하며 공개 이력은 정리한 소스 스냅샷에서 시작한다. 서명 정보는 Git에서 제외한 `macos/Signing.local.xcconfig`에 둔다. 스키마의 관계 식별자는 `collection://local-<table>`로 통일한다.
