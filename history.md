# 현재 상태와 검증 기록

## 2026-09-17 — UX 개편 PRD 승인 반영

- 사용자가 전체 UX 검토 방향을 수용하고 PRD 갱신을 요청했다. 환자 중심 작업 화면, 자동 저장·동시 작업, 재진·과거 기록의 3단계로 PRD를 재구성했다.
- UX-01~10과 AC-01~15로 요구사항·단계·완료 조건을 연결했다. 파일 전사 유지, 마이크 음성 파일 미생성, MVP 비차단과 데이터 보존을 공통 조건으로 명시했다.
- 현재 구현과 목표를 구분했다. 미동의 시 CRM 초안을 막는 기존 AI 지침은 1차 개선 대상으로 명시했으며, 이번에는 코드 수정·시험 실행·DB 변경·재시작·커밋을 하지 않았다.
- 문서의 요구사항/수용 기준 ID, 참조 파일, 단계별 범위와 보존 조건을 확인했다. PRD·history 외 기존 추적 파일은 작업 전과 동일하다.

## 2026-09-17 — Git 이후 코드·문서 정리

- 이전 구현·조사·실험 기록은 비공개 Git 보관본에 남긴다. 이 문서의 과거 커밋 해시는 공개 이력에서 조회되지 않는다.
- 사용하지 않는 `approval.py`, `sigma.py`, `webhook.py`와 해당 전용 테스트 14개를 제거했다. 외부 연동은 현재 로컬 앱에 연결되어 있지 않았다.
- `survey.py`의 별도 서버·별도 DB 생성·CLI를 제거했다. 설문 화면/검증은 유지하고 HTTP 출처 검사·토큰·타임아웃은 통합 `app.py`로 옮겼다. 설문 회귀 테스트 7개는 통합 앱 대상으로 전환했다.
- 기본 포트는 8766이다. 앞서 승인받아 종료한 8765는 닫힌 상태다. 현재 시험 DB를 지정하여 8766 서버를 재시작했다.
- `Agent.md`, `PRD.md`, 이 파일을 현재 제품·실행·검증 중심으로 정리했다. 앱에서 사용하는 원본 스키마와 스킬 문서 JSON은 유지했다.
- DB·QA 원문·기존 오디오·빌드 산출물은 삭제하지 않았다. 기본 DB의 7개 테이블은 모두 0건이다. 현재 시험 DB는 surveys/exams/sessions/crm 각 1건, 나머지 0건이며 기존 건수를 유지했다. 세 DB의 integrity_check=ok.

### 이번 실행 검증

- `python3 -m unittest discover -s tests -q`: 35개 통과. 기존 49개 중 폐기한 외부 연동 전용 14개만 제외했다.
- `python3 -m scripts.smoke_ui`: DOM 기반 7개 목록·CRUD·검사·세션 문서/검토·CRM·문서 열람 통과. 실제 브라우저 화면 재시험은 수행하지 않았다.
- `python3 -m scripts.smoke_live_speech .build/qa-case/synthetic.aiff`: 실제 Apple 엔진, 12초 메모리 PCM, 중간 결과 확인, 최종 23구간, 생성 파일은 임시 DB만, 오디오 파일 생성 0.
- 재시작 후 `/health`, `/survey`, `/api/bootstrap`, `/api/speech`, `/api/model`, `/app.js`, `/mic.js` 모두 HTTP 200. AFM 사용 가능/8192 문맥과 ko_KR 설치 상태 확인.
- 재시작 시 Store의 `PRAGMA user_version` 실행으로 SQLite 파일 헤더가 갱신되어 시험 DB의 바이트 해시는 바뀐다. 별도 DB 사본에서 같은 초기화 전후 SQL dump가 동일함을 검증했다. 기본 DB 두 개는 바이트 해시도 유지했다.
- 현재 변경은 아직 커밋하지 않았다. 원격 연결·push 없음.

## 유지하는 이전 검증과 한계

아래는 첫 커밋 이전 기록 요약이며 이번 실행 결과와 구분한다.

- 인앱 실제 마이크: 사용자 권한 허용 후 약 37초/27구간 전사, 정지 후 기존 본문 이어쓰기, 취소 후 기존 저장 전사 유지, 네이티브 프로세스 종료 확인.
- 파일 업로드→Apple 한국어 전사→로컬 AFM SOAP 생성은 합성 음성으로 확인했다. 파일 업로드 기능과 관련 회귀 테스트를 유지한다.
- 지정 Notion 사례의 별도 QA 데이터로 설문·세션·검사·AI 생성·CRM 화면을 시험했다. 개인정보/동의/승인 상태에 따른 로컬 MVP 업무 차단은 사용자 결정으로 해제했다.
- 실제 AFM에서 부정문 반전과 SOAP 항목 혼합을 관측했다. 생성 기능 성공은 임상 정확성 보증이 아니다. 실제 진료 용어·겹침 발화·장시간 정확도와 화자 역할 분리는 검증되지 않았다.

## 2026-09-17 — UX 원샷 실행 loop

### 원샷프롬프트 v1 (실행 기준)

```text
저장소 루트에서 PRD.md의 UX-01~10, AC-01~15를 모두 구현하고 직접 검증한다.
기존 미커밋 변경·7개 업무 테이블·행 ID·본문·관계·전사 이력을 보존한다. 마이크 오디오는 파일로 만들지 않고 기존 파일 전사를 유지한다.
환자 작업 화면, 항목 단위 자동 저장/충돌 복구, 비동기 AI 후보/부분 적용/버전 복원, 로컬 환자 ID와 방문 연결을 하나의 일관된 흐름으로 구현한다.
MVP 업무 상태는 생성·저장을 막지 않으며 외부 발송·동기화·AI fallback을 추가하지 않는다. 사용자 승인된 인앱 검증은 로컬 합성 시험 범위에서 수행한다.
매 회차 실행→결과 관측→AC별 평가→실패 원인을 해결하도록 이 지시 보완 순서로 반복한다. 기준을 낮춰 통과시키지 않는다.
모든 AC의 직접 증거가 확보되기 전 완료/100%라고 보고하지 않는다. 장애는 사실대로 남기고 독립적으로 가능한 작업을 계속한다.
실행·변경·검증·평가와 프롬프트 버전은 history.md에 기록한다. 코드·데이터를 외부로 전송하거나 push하지 않는다.
```

### 회차 1 — 기반 구조와 전체 흐름 구현

- 시작 상태: main / 210b2d7, 이전 정리·PRD 변경이 미커밋 상태. 이를 보존한다.
- 기존 시험/빈 DB를 `.build/ux-loop/baseline-qa.sqlite3`, `baseline-empty.sqlite3`에 SQLite backup으로 보존했다. 시험 자료와 백업은 Git 제외 경로다.
- 3차 데이터 결정: 새 patient ID를 세션별로 부여하고 동명이인·같은 이름을 자동 병합하지 않는다. 기존 행은 바꾸지 않고 별도 방문 연결 테이블을 둔다. 명시적 연결 정정과 새 방문은 사용자 행동으로만 수행한다.
- AC 상태: AC-01~15 구현/검증 대기. 이전 MVP 시험을 새 UX 통과로 계산하지 않는다.

### 회차 1 관측 → 원샷프롬프트 v2

- 구현: 환자 작업 탭·환자 요약·검사 연결·안내 구조화, 항목별 저장·로컬 초안 복구, 백그라운드 AI 후보, 편집 이력, 환자 ID/방문 매핑을 추가했다. 아직 전체 수용 판정 전이다.
- 기존 35개 회귀 중 전사 동시 수정 기대 1개가 실패했다. 기존 테스트는 메모만 바뀌어도 전사 저장을 거부했으나 PRD는 동시 메모를 요구한다. 테스트를 ‘다른 항목은 병합’, ‘같은 전사 본문은 충돌·복구’로 분리한다.
- v2 보완 지시: v1을 유지하고 실제 동시 저장·작업 중 입력 변경·재생성 후보·동명이인·기존 DB 이관을 집중 시험한다. 기존 검사를 단순 삭제하지 말고 새 요구사항의 긍정/실패 경로로 대체한다. DOM 시험도 새 탐색 구조로 갱신하되 7개 기록 관리·파일 전사·CRM·오류 보존 검증을 유지한다.

### 회차 2 결과 → 원샷프롬프트 v3

- 저장소 시험에서 AI 작업 INSERT의 자리표시자 수 오류 2건을 발견·수정했다. 수정 후 전체 48개 단위/HTTP 시험이 통과했다.
- 새 DOM 통합 시험 통과: 대기 목록 검색, 검사 자동 연결, 자동 저장, 생성 중 메모, 후보 일부 적용, 복사, 메시지 자동 연결, 같은 항목 충돌 해결, 네트워크 실패 후 재로딩 초안 복구, 버전 조회, 재진/과거 기록, 마이크 권한 거절.
- 실제 SpeechTranscriber 시험: 12초 PCM, 중간 결과와 최종 23구간, 전사 중 의사 메모 병합 성공, 생성 오디오 파일 0.
- 추가 발견: 2초 작업 조회가 열린 후보 비교를 접어 버릴 수 있어 상태 변화가 있을 때만 갱신하고 펼침 상태를 보존하도록 수정했다. 적용 결과에 입력 출처/변경 여부도 기록한다.
- v3 보완 지시: v2를 유지하고 실제 AFM a/b/c 작업 큐, 구조화 안내 결과, 마이크 권한/해제, 1280×720와 좁은 화면, 후보 비교의 열림 유지, 회귀 파일 전사를 검증한다. DOM 통과를 GUI·실제 엔진 증거로 대체하지 않는다.

### 회차 3 관측 → 원샷프롬프트 v4

- 실제 파일 전사 회귀: `python3 -m scripts.smoke_speech` 통과. 합성 파일 12.95초, 22개 시간·신뢰도 구간, Apple Speech→HTTP→SQLite→실제 AFM SOAP까지 연결됐고 전사 원본을 보존했다. 임시 파일은 TemporaryDirectory 종료 시 제거된다.
- 코드 대조에서 저장 응답 대기 중 새로 바꾼 진행 상태와 다른 항목의 기준값이 갱신될 수 있는 경쟁 조건을 발견했다. 전송 시점의 상태와 항목별 기준값을 보존하도록 수정했다. AI 적용 출처도 선택한 항목만 연결한다.
- 빠른 화면 이동 시 늦게 도착한 이전 화면 응답을 무시한다. 좁은 화면의 참고 패널은 접어서 시작한다. 이전 Notion 지시문 형식 요약은 기본 화면 대신 원문 상세에 남긴다.
- v4 추가 지시: “저장 중 추가 편집과 빠른 탐색을 지연 응답으로 재현하고, 후보 출처·복원 버튼·텍스트 입력·DB 백업 복구를 실제 동작으로 검증하라. 화면 검증은 기존 시험 DB의 별도 사본에서 수행하라. 각 AC의 일부 증거만으로 통과 판정하지 마라.”

### 회차 4 — 실제 입력·화면 및 복구 시험

- 인앱 8768은 `.build/ux-loop/gui.sqlite3` 사본을 사용했다. 새 origin의 마이크 허용 응답이 필요했고 사용자가 허용했다고 답한 뒤 실제 인식이 시작됐다.
- 실제 마이크: 44.608초, 53개 구간. 입력 감지·한국어 전사 중 메모 입력→기록 검토 탭 이동→정지→같은 진료에 전사/메모 저장을 확인했다. 별도 두 번째 실행은 취소했고 본문 길이 787자와 메모가 그대로 남았다. `audio_storage=none`, 작업 폴더 오디오 0. 원문은 Git·이 문서에 복사하지 않는다.
- 1280×720 첫 화면: 환자/주소증/방문/4개 작업 탭/대표 행동과 측정 요약을 확인했다. 390px 설문: innerWidth=scrollWidth=390. 복용약 있음은 상세 필수, 없음은 상세 숨김·값 없음, Tab으로 다음 항목 이동, 오류 시 이름 답변 유지와 휴대폰 포커스를 확인했다.
- GUI에서 실제 사례 SOAP 생성 중 환자 안내 탭으로 이동할 수 있었고, 완료한 후보와 기존 편집본을 나란히 열었다. 후보는 자동 적용되지 않았다.
- 지연 응답 DOM 회귀 통과: 늦은 화면 응답 폐기, 저장 중 추가 상태/진찰 편집 보존, 텍스트 파일 입력, 실제 복구 버튼 적용, 충돌 양쪽 비교, 네트워크 실패 초안 재로딩 복원. 전체 단위/HTTP 시험 50개 통과.
- 추가 보완: 이전 버전의 빈 항목도 복구할 수 있게 했다. AI 후보 비교에 현재 편집값을 갱신하고 상단에서 작업 상태/후보 위치로 이동하도록 했다. 명시적 초기화 명령의 백업과 삭제 범위에 새 보조 저장 구조를 포함했다(기존 DB에 초기화 실행하지 않음).
- v5 추가 지시: “빈 항목 복구·초기화 백업·AI 출처 범위를 회귀 검사하고, 최종 코드로 전체 시험과 실제 AFM을 실행한다. 기록된 과거 통과로 최종 코드를 대신하지 않는다. 원래 DB 7개 테이블과 기본 빈 DB를 대조하고 최종 AC별 증거를 작성한다.”

### 회차 5 관측 → 원샷프롬프트 v6

- 전체 51개 단위/HTTP 시험과 DOM 지연 응답·장치 종료 모의 시험 통과. 실제 AFM 3단계는 6/7/7초에 후보를 반환하고 부분 적용·미동의 안내 저장까지 성공했다.
- 다만 실제 AFM 안내 한 건에서 여러 증상에 걸린 ‘없음’을 일부 ‘나타남’으로 반전했다. 이는 기능 통과와 별도의 임상 품질 실패이며 숨기거나 정확성 통과로 계산하지 않는다. 기존 PRD의 수치/부정문 원문 대조와 검토용 초안 원칙을 유지한다.
- 로컬 생성 지침을 보완했다: 나열된 부정 표현의 적용 범위를 명시하고, 환자 안내는 제공된 설명·계획 중심으로 작성하며 불필요한 병력 재요약을 덧붙이지 않는다. 원본 Notion 스킬의 최신 검사 구분·미검사·근거 없음·확정/보장 금지 규칙도 압축 지침에 명시했다.
- v6 추가 지시: “최종 모델 지침으로 안내 3개 필드를 직접 읽고 재시험하라. 유한한 기능 시나리오의 AC 충족과 모델의 모든 임상 출력 100% 정확성을 구분하라. 확인하지 않은 임상 성능을 통과로 보고하지 마라.”

### 회차 6 — 최종 수용 평가

- 최종 로컬 AI 재시험: AFM 3 Core Advanced / available / 8192, a 9초·b 10초·c 6초. 후보 저장, 사용자 S 보존, O/A/P 부분 적용, 구조화 안내 3필드 및 미동의 로컬 CRM 저장이 동작했다. `rx_guide`의 한약 처방 없음은 해당 없음으로 나왔다.
- 같은 재시험에서도 안내문/메시지의 부정 표현 반전과 ‘3일 뒤’→‘3일 전’ 오류가 남았다. **임상 문장 정확성은 미통과**다. 지침만으로 해결됐다고 보고하지 않는다. 초안을 적용하기 전 원문·SOAP·설명과 사람이 대조해야 한다. 새 임상 정확도 검증 체계를 만들거나 외부 모델로 바꾸는 것은 이번 UX PRD 범위가 아니다.
- 최종 단위/HTTP: `python3 -m unittest discover -s tests -q` — 51개, 8.689초, OK.
- 최종 DOM: `python3 -m scripts.smoke_ui` — 실제 임시 HTTP/SQLite와 jsdom, 작업 흐름·상태 필터·검사·동시 저장·충돌·초안 재로딩·버전 복구·텍스트 가져오기/내보내기 통과. 마이크 장치 연결 종료와 페이지 이탈의 track/context 정리는 모의 장치로 검증하고 실제 마이크/엔진 증거와 구분했다.
- 원래 시험 DB의 7개 테이블을 baseline 사본과 다시 읽어 비교: ID·속성·본문·revision·관계·전사 이력을 포함한 모든 행 동일. 시험 DB는 surveys/exams/sessions/crm 각 1건, 나머지 0건. 기본 DB는 7개 모두 0건. 시험/기본/이전 빈 DB의 integrity_check=ok.
- 최종 runtime: 원래 시험 DB로 8766 재시작. `/health`, `/survey`, `/api/bootstrap`, `/api/workspace`, `/api/speech`, `/api/model`, `/app.js`, `/mic.js`, `/survey.js` 모두 HTTP 200. 8765와 사본 검증용 8768 종료. 인앱은 8766 기존 세션으로 복귀했고 viewport 시험 설정을 해제했다.
- 검사: `git diff --check`, JS 구문 검사 통과. 현재 main / 210b2d7 위 미커밋 변경. 기존 미커밋 정리 작업을 보존했고 commit/push/외부 발송은 하지 않았다.

| 수용 기준 | 최종 기능 판정 | 이번 실행의 근거 |
| --- | --- | --- |
| AC-01 | 통과 | DOM 전체 진료 흐름과 GUI 작업 탭 이동. 검사·SOAP·안내에 별도 DB 메뉴/환자 재선택 없음 |
| AC-02 | 통과 | 실제 1280×720 첫 화면과 DOM 검색·상태 칩/필터·문진 바로가기 |
| AC-03 | 통과 | 같은 방문의 검사 입력→자동 관계→120/80 요약 갱신, 기존 검사 단위·미입력 처리 회귀 |
| AC-04 | 통과 | 390px 설문 있음/없음 조건·오류/답변 유지, 서버 형식 검사·동시/중복 제출 회귀 |
| AC-05 | 통과 | S/O/A/P 편집·현재 편집본 복사, 옆 설문·검사·전사 원문 대조 UI |
| AC-06 | 통과 | 실제 AFM 구조화 3필드, 후보 항목 적용→메시지 자동 연결→수신자·로컬 CRM 저장. 임상 문장 정확성은 위 실패 기록과 구분 |
| AC-07 | 통과 | 미동의·미승인·발송일 공란의 서버/DOM/실제 AFM 생성·저장, 처방 없음의 해당 없음 |
| AC-08 | 통과 | 인앱 실제 44.608초 마이크·메모·작업 탭 이동·정지/취소. 권한 거절/장치 종료/페이지 이탈 회귀, 오디오 파일 0 |
| AC-09 | 통과 | 지연 응답 중 추가 수정, 저장 실패 표시·localStorage 초안 재로딩 복구, SQLite 재시작/복원 |
| AC-10 | 통과 | 생성 입력 스냅샷·stale 표시·생성 중 메모/이동, 기존 수정본 보존, 적용 출처 항목 범위 검사 |
| AC-11 | 통과 | GUI 실제 O만 적용하고 S 보존, DOM 현재 SOAP 복사/내보내기·이전 항목 복구 |
| AC-12 | 통과 | 실제 합성 파일 HTTP 업로드→Apple 전사→AFM, 임시 파일 정리, 텍스트 가져오기 및 원문·시간·이력/오류 보존 회귀 |
| AC-13 | 통과 | 동명이인 별도 ID·명시적 재연결·새 방문·과거 기록, 전체 DB 백업 복원 후 환자/방문/문서/이력 동일 |
| AC-14 | 통과 | 실제 설문 width=scrollWidth=390, Tab/포커스·레이블·항목 옆 오류, 390px 직원 참고 패널 닫힘, 텍스트 상태 |
| AC-15 | 통과 | 원래 DB와 baseline의 7개 테이블 동일, 기존 세션 URL, 3개 DB 무결성, 빈 DB 샘플 0 |

**판정 범위:** PRD의 유한한 UX·저장·복구·입출력 수용 시나리오는 15/15 통과했다. 이는 모든 진료 데이터에 대한 정확성 100%나 무결점 제품을 의미하지 않는다. 이 실험에서 확인된 AI 임상 내용 오류는 해결되지 않은 한계로 남는다.

재현 자료는 Git 제외된 `.build/ux-loop/`의 baseline SQLite 사본, GUI 시험 DB, `ai-result.json`, `final-runtime.json`에 있다. 이 문서에는 실행 사실과 비식별 결과만 기록한다.


## 2026-09-17 — AFM Cloud / Cloud Pro 확장 loop

- 목표: 기존 가/나/다 후보·부분 적용 흐름에 명시적 모델 선택을 추가한다.
- 범위: 공통 프롬프트·Shortcuts 어댑터·작업 큐·화면·두 전용 단축어. 사용자 승인으로 단축어 앱 가져오기·모델 설정 확인을 수행했다.
- 완료 조건: 세 모델 실제 입출력, 필수 필드 검증, 실패 보존, 모델/프롬프트 출처, 인앱 흐름과 합성 비교 증거.
- 제외 범위: 기존 진료 자료 Cloud 전송·자동 fallback·원격 게시·환자 발송. 기존 미커밋 UX·정리 변경을 보존한다.
- 검증: 단위/HTTP/DOM + 실제 CLI Cloud/Cloud Pro와 Swift 로컬 + 정답 전사/Apple ASR 합성 비교. `.build/cloud-loop/baseline-qa.sqlite3`, `baseline-empty.sqlite3`에 변경 전 SQLite 사본을 보존했다.

### C1 — 단축어 연결과 입출력 수정

- 기존 `AFM Cloud 연결 시험`은 고정 `1+1은?` / Apple Intelligence Pro 설정이었다. 그대로 두고 `AFM Cloud 호출`, `AFM Cloud Pro 호출`을 새로 가져왔다. UI에서 각각 클라우드 / Cloud Pro 선택을 확인했다.
- 최초 `public.utf8-plain-text` 요청은 exit 0이었지만 반환 파일이 없었다. 표준 출력 관찰에서 응답이 RTF였으며 JSON의 밑줄이 서식 처리에 소비되는 것을 확인했다. 단순 성공 코드로 통과 처리하지 않았다.
- 명시적 결과 출력, 모델 출력=텍스트, CLI `public.plain-text`, JSON 코드 펜스 지침으로 수정했다. 고정 프롬프트·빈 값·이전 요청 응답은 요청별 nonce/구조 검사에서 실패한다.
- 두 모델 실제 nonce 왕복 재시험: Cloud 1.4초, Cloud Pro 1.5초. 모두 `request_id` 일치와 `CLINIC_NONCE_OK`를 반환했다. `.build/cloud-loop/handshake-final.json`에 비식별 증거를 기록했다.
- PRD 원샷프롬프트 v7: 명시적 모델 선택과 전송 위치, 같은 입력/프롬프트 고정, 기능·임상 평가 분리, 실패/재시도/출처 검증을 추가했다.


### C2 — 실제 3모델 비교 → 프롬프트 보완

- 첫 비교는 동일 단계 입력 fingerprint를 고정한 12회 실행 중 11회 구조 통과, Cloud 가 요약 1회 실패였다. 원문 응답 확인 결과 JSON 대신 일반 문서였다. 같은 GUI 요청도 실패했으나 편집본 SOAP는 그대로 보존됐다.
- 출력 키를 금지한 것으로 해석할 수 있는 지침을 ‘각 필드 본문에 내부 지시를 쓰지 않음’으로 명확히 했다. 출력 계약을 입력 앞뒤에 배치하고 JSON escape·실행 ID를 명시했다. 프롬프트 `clinic-2026-09-17-cloud-v2`, 출력 계약 `clinic-output-v2`로 구분한다.
- Cloud 큐에는 CLI로 보낼 완성 텍스트·요청 ID까지 저장하여 대기 중 코드/설정 변경이 입력을 바꾸지 않도록 했다. 실제 단축어 동작 변경은 해시 비교에서 오류로 남긴다.
- 수정 후 Cloud 가 단독 재시험은 JSON/요청 ID 검사를 통과했다. 처음 비교 결과는 `.build/cloud-loop/comparison-c1.json`에 그대로 보존한다.
- 첫 임상 내용 검토: 로컬 ASR 입력 결과에 ‘방사통·저림·근력저하가 있습니다’라는 부정문 반전이 있었다. 로컬 content에는 내부 작성 지시도 섞였다. Cloud Pro 정답 전사 결과의 content에 ‘무거운 물건’(입력은 상자)이 추가되고 A에 요부 염좌 의심을 추론했다. 이 관측을 임상 정확성 통과로 계산하지 않는다.
- 실제 Apple ASR 합성 33.66초에서는 ‘저림→절임’, ‘압통→앞통’과 문장 분리 변화가 나타났다. 모델 평가에서 정답 전사와 ASR 경로를 별도로 표기한다.


### C3 — JSON 불안정 재현과 Shortcuts 출력 계약 수정

- v2 최종 비교는 11/12 구조 통과였다. Cloud의 실제 ASR 입력 SOAP 응답이 문자열 안에 escape되지 않은 줄바꿈을 넣었다. v3에서는 각 값을 한 줄로 작성하도록 했지만 다른 Cloud 요약이 다시 일반 텍스트를 반환해 11/12였다. 세 회차 결과를 c1/c2/c3 파일로 보존했다. 반복 프롬프트 수정으로 신뢰성을 확보했다고 판단하지 않았다.
- 단축어 앱의 모델 동작에는 `텍스트/사전` 출력 선택이 있었다. Cloud만 사전(Dictionary)으로 바꿨다. 사전은 `public.plain-text`로 반환되지 않았고 `public.json`으로 지정했을 때 실제 JSON 파일을 반환했다. Cloud Pro는 검증된 텍스트/public.plain-text를 유지한다.
- 연결 스크립트가 Cloud Dictionary / Pro Text를 각각 확인하고 고정 UUID·동작 해시를 갱신한다. 생성 스크립트도 동일한 구성을 재현한다. 실행 중 임의 전환은 없다.
- 최종 프롬프트 버전은 `clinic-2026-09-17-cloud-v3`, 출력 계약은 `clinic-output-v3`다. PRD 원샷 v9는 모델 출력 유형과 CLI UTI까지 실제 증거로 검증하도록 보완했다.

### C4 — 인앱 기능 및 데이터 보존

- 별도 `.build/cloud-loop/gui.sqlite3`에 코드로 만든 합성 환자만 넣고 8768에서 검증했다. 기존 환자 DB/Notion 자료는 Cloud 시험에 사용하지 않았다.
- GUI 실제 Cloud 재시도: 선택 상자는 Cloud Pro였지만 ‘AFM 3 Cloud로 재시도’ 버튼은 Cloud를 호출했고 v2 후보를 반환했다. Cloud Pro SOAP 생성 중 문진 탭 이동을 확인했다. 후보에서 O만 적용한 뒤 S와 A/P 기존 편집본이 보존됐고 O 출처에 Cloud Pro가 표시됐다.
- 1280×720 CSS px 직원 화면에서 모델 선택·처리 위치·환자/작업 탭·검사 요약 배치를 확인했다. 390px 화면은 scrollWidth=innerWidth=390이며 모델 선택과 위치 안내가 줄바꿈되고 가로 넘침이 없었다. 화면 크기 시험 설정은 해제했다.
- 원래 8766 DB에는 작업 시작 사본 이후 전사 저장 1건(revision 38→39, 06:34 KST)이 별도로 관측됐다. 이 변경을 보존했다. 원래 4개 전사 이력과 이전 원문이 새 5개 이력에 보존됐고 나머지 문서/속성·다른 6개 테이블은 동일했다. 이번 시험은 별도 합성 DB에서 수행했다. runtime 반영 직전에 최신 DB를 다시 백업해 대조한다.


### C5 — 최종 검증과 평가

- 최종 실제 비교 `python3 -m scripts.smoke_model_comparison`: 정답 전사 가/나/다×3모델과 실제 ASR 전사 나×3모델, **12/12 후보 생성·구조 검증 통과**. 단계별 입력 fingerprint 동일, 후보가 기존 문서를 자동 적용한 횟수 0. `.build/cloud-loop/comparison.json`에 모든 응답과 설정·시간을 기록했다.

| 모델 | 실제 구조 통과 | 가 / 나 / 다 / ASR 나 (초) | 내용 직접 검토 |
| --- | --- | --- | --- |
| Core Advanced | 4/4 | 5.1 / 9.4 / 5.1 / 15.5 | 내부 지시문 복사, 환자 진술/진찰 혼합. ASR SOAP에서 세 증상의 부정 표현 누락. 안내문은 계획 대신 병력만 재요약 |
| Cloud (사전 출력) | 4/4 | 3.1 / 3.5 / 2.7 / 3.7 | 이번 사례의 부정문·수치·HRV 미검사·진단 미확정·3일 뒤·한약 처방 없음 보존. 세미콜론 중심으로 다소 기계적인 문체 |
| Cloud Pro (텍스트 출력) | 4/4 | 2.7 / 2.9 / 3.3 / 2.7 | 구조/핵심 수치·부정 표현 보존. ‘무거운 물건’, ‘무리한 활동을 피함’ 등 미제공 내용 추가, ASR A에 요추부 염좌 의심 추론 |

- 속도는 이 Mac/네트워크의 각 1회 소표본이며 일반적인 성능 우열을 입증하지 않는다. 안내 전 원문 검토는 세 모델 모두 필요하다. **이번 연결의 기능 수용은 통과, 임상 정확성 100%는 미충족**이다. 원자료에 없는 추론을 자동 삭제하거나 정상 판정으로 숨기지 않았다.
- 마지막 전체 회귀: `python3 -m unittest discover -s tests -q` — **65개 / 8.718초 / OK**. `python3 -m scripts.smoke_ui` — 기존 UX 흐름과 명시적 모델 선택, 선택만으로 생성하지 않음, Cloud 출처, 모델 변경 후 원래 모델 재시도 통과. JS 구문 및 `git diff --check` 통과.
- 연결 설정 회귀에서 고정 문자열 프롬프트를 토큰 객체로 읽던 오류를 발견했다. 설정 도구가 형식 오류를 설명하고 기존 연결 파일을 보존하도록 수정한 뒤 전체 시험을 다시 통과했다.
- 현재 8766 runtime을 최신 코드/기존 시험 DB로 재시작했다. 진행 중 AI 작업과 음성 자식 프로세스가 없는 것을 확인하고, `.build/cloud-loop/before-runtime.sqlite3`에 당시 최신 데이터를 백업했다. 재시작 전후 7개 테이블 모든 행 동일, integrity_check=ok, schema version=3. 기본 DB 7개 테이블 모두 0건.
- `/health`, `/survey`, `/api/bootstrap`, `/api/workspace`, `/api/models`, `/api/model`, `/api/speech`, `/app.js` 모두 200. 세 모델 구성 표시, 실제 모델 상태, 전사 API 응답을 확인했다. `final-runtime.json`에 결과를 보존한다.
- 기존 `AFM Cloud 연결 시험`의 1+1/Pro 한 동작은 그대로다. 새 전용 2개만 추가·설정했고 재설치용 서명 파일도 최종 구성으로 다시 만들었다. 단축어 동작 해시와 연결 파일 일치 확인. 단축어 앱 DB에는 읽기 전용 SQL만 사용했다.
- Git은 main / 210b2d7 위 미커밋 상태다. 이전 UX·정리 변경을 보존했다. 이번 변경을 commit/push하지 않았다.

| 확장 기준 | 판정 | 최종 근거 |
| --- | --- | --- |
| AI-01 | 통과 | 실제 nonce 왕복, 최종 각 모델 4회 구조 검사 |
| AI-02 | 통과 | DOM 선택/자동 요청 없음, GUI 처리 위치·후보·O 적용 출처, 큐 요청 고정 회귀 |
| AI-03 | 통과 | 실제 Cloud 형식 실패 시 보존·명시적 재시도, timeout/잘못된 JSON/불완전 SOAP/설정 변경/임시 파일 정리 단위 시험 |
| AI-04 | 통과 | 65개 회귀·전체 DOM, 기존/빈 DB 대조, Speech 실제 합성 전사. 실시간 마이크 코드와 오디오 저장 경로는 이번 확장에서 변경하지 않음 |
| AI-05 | 통과 | 동일 입력 12회와 정답/실제 ASR 분리, 소표본 임상 내용 오류를 별도 기록 |


- 최종 v3 + Cloud 사전/public.json 구성으로 인앱 ‘요약 만들기’를 한 번 더 실행해 후보 준비를 확인했다. 시험 탭과 8768 서버는 종료했다. 원래 8766 세션을 새로고침하여 세 모델 선택 상자·저장됨 상태를 확인하고 화면을 남겼다. 이 원래 세션에서 Cloud 생성을 실행하지 않았다.

## L — 실시간 전사 피드백과 읽기 중심 화면 (2026-09-17)

### L1 — 실행 계획과 첫 관측

- 승인 범위: 사용자가 수용한 단일 전사 본문, 회색 중간/검은 확정 구분, 즉각적인 마이크 입력 표시, 조작 축소, SOAP 항목별 편집. 기존 파일 전사·메모·저장·복구 유지. PRD 원샷을 v10으로 갱신하고 UX-L1~4를 추가했다.
- 시작 시 main의 기존 미커밋 변경을 보존했다. `.build/live-ux/before.sqlite3`와 `empty-before.sqlite3`에 현재/빈 DB를 백업했다. 시험은 임시 DB 및 별도 `gui.sqlite3`/8768에서 진행했다.
- 첨부 영상은 로컬 프레임으로 확인했다. 외부 앱의 회색→검은색 표시를 LLM 교정 증거로 해석하지 않았다.
- 최초 실제 12초 PCM 실시간 속도 시험: 기존 `.volatileResults`만 사용한 엔진의 첫 중간 결과 11.603초, 송신 중 임시 결과 변경 1회. 500ms UI 조회만으로 설명할 수 없는 엔진 지연을 확인했다.

### L2 — 변경과 재시험

- Swift 실시간 브리지에 `.fastResults`를 추가했다. 파일 전사 브리지 설정은 유지했다. Apple은 이 옵션을 빠른 응답/정확도 절충으로 설명한다: [ReportingOption](https://developer.apple.com/documentation/speech/speechtranscriber/reportingoption).
- 화면은 기존 저장 본문, 이번 확정 구간, 회색 임시 끝부분을 한 영역에 표시한다. 임시 문장은 추가 누적하지 않고 교체한다. 검은색은 음성 인식 확정이며 임상 검토/LLM 교정 완료를 뜻하지 않는다. 종료하면 저장한다.
- Web Audio AnalyserNode+requestAnimationFrame으로 음량에 반응하는 파형을 표시한다. 서버 결과가 없어도 작동한다. 실제 입력이 없으면 파형을 임의로 움직이지 않는다. PCM 전송은 기존 200ms 단위 메모리 경로이며 활성 마이크의 결과 조회만 250ms로 줄였다.
- 결과 수신 때 전체 도크를 다시 만들지 않아 버튼 포커스/열린 메뉴를 유지한다. 긴 전사는 본문 내부에서 스크롤하고, 끝부분을 보고 있을 때만 새 문장을 따라간다. 이전 구간을 읽는 동안 자동으로 끌어내리지 않는다.
- 마이크 시작/종료를 하단 한 곳에 모았다. 취소와 해당 진료 링크는 추가 메뉴, 파일/텍스트 입력은 가져오기 메뉴로 옮겼다. 전사는 읽기 기본이며 마이크 종료 후 수정한다. 의사 메모는 펼쳐 입력하며 동시 저장 가능하다. SOAP S/O/A/P는 읽기 기본·개별 수정·자동 저장을 사용한다.
- 두 번 빠르게 시작하는 요청을 하나로 제한했다. 종료된 마이크의 늦은 오류가 새 마이크를 취소하지 않도록 대상 일치를 확인한다.
- 합성 GUI 자료를 처음 만들 때 필수 설문 항목 누락이 검출됐다. 필수 항목을 채워 다시 만들었으며 유효성 검사를 약화하지 않았다.

### L3 — 최종 검증 증거

| 기준 | 현재 결과 | 근거 |
| --- | --- | --- |
| UX-L1 즉각적 입력 피드백 | 통과 | DOM에서 ASR 응답을 1.2초 지연시켜도 그 전에 파형 활성화·본문 미수신을 확인. 인앱 실제 마이크 준비→듣기→종료 확인. 실제 마이크 무음 시험은 입력 반응의 밀리초 측정이 아님 |
| UX-L2 임시/확정 구분 | 통과 | DOM 임시 교체→확정 이동→다음 임시 구간, 저장 본문 보존·포커스 유지. 실제 엔진 중간 결과 수신 및 종료 후 확정 저장 |
| UX-L3 읽기 기본·편집 | 통과 | 인앱 전사 본문 수정→저장, SOAP O만 수정, 동시 의사 메모 저장. 1280px 직원 화면 관측, 390px scrollWidth=innerWidth 및 도크/종료/추가 메뉴 화면 내 배치 |
| UX-L4 기존 경로/복구 | 통과 | DOM 권한 거부·장치 유실·페이지 이탈·복구·중복 시작·탭 이동·파일 가져오기. 인앱 실제 마이크 중 메모/탭 이동·무음 종료·다시 시작/취소. 실제 파일 전사와 이전 본문 이력 보존 |

- 실제 엔진 비교 재실행: `python3 -m scripts.smoke_live_speech .build/qa-case/synthetic.aiff --realtime --bridge .build/live-ux/standard-speech --report .build/live-ux/standard.json`, 같은 입력에 기본 빠른 브리지를 사용해 `fast.json` 생성. 기존 11.606초/1회 → 빠른 설정 1.206초/12회(첫 중간 결과/12초 송신 중 중간 변경). 두 설정 각각 총 2회 실행에서 유사한 결과였다. 모델 준비 후 PCM 전송 시작을 기준으로 한 API 관측이며 브라우저에서 실제 발화 시작→화면 표시를 측정한 수치가 아니다.
- 12초 송신 중 확정 결과는 관측하지 못했다(`first_final_seconds=null`). 종료 후 확정 전사·시간 정보·기존 전사 이어쓰기·메모 보존 검증은 통과했다. 생성 파일은 시험 DB뿐이며 마이크 오디오 파일 0개.
- 최종 비교 전사에서 두 설정 모두 ‘의사’ 오인식이 있었고, 빠른 설정에는 추가 단어 오류가 있었다. `.fastResults`의 정확도가 동일하다고 판단하지 않는다. 임상 용어/수치/부정 표현 정확성과 장시간 진료 성능은 미검증이다. 자동 LLM 교정·화자 지정·Cloud 전송은 추가하지 않았다.
- 인앱 가져오기 메뉴에서 기존 합성 AIFF를 실제 Apple 파일 전사로 처리했다. 도구 클릭 응답이 시간 초과했으나 재실행하지 않고 화면/DB를 확인하여 성공을 판정했다. 153개 시간 구간, 원본 파일 SHA 일치, 이전 편집 본문 이력·의사 메모·SOAP O 보존을 별도 API로 확인했다.
- `python3 -m unittest discover -s tests -q`: 65개 / 8.672초 / OK. 최종 `python3 -m scripts.smoke_ui`: 기존 UX·Cloud 선택/재시도 회귀 및 이번 전사/읽기 편집/지연 응답 시험 통과. JS 구문 검사·`git diff --check` 통과.

### L4 — 데이터와 runtime

- 원래 8766 DB에는 작업 시작 이후 별도 전사 추가가 관측됐다(revision 39→40, 07:22 KST). 처음 전체 행 동일성 검사는 이 변경 때문에 실패했다. 변경 내용을 되돌리지 않고 항목별 대조로 전환했다. 기존 전사 전체가 새 본문의 prefix로 보존됐고, 기존 5개 이력과 직전 원문이 6개 이력 안에 보존됐다. 다른 문서/속성·다른 6개 테이블은 동일하다. 기본 빈 DB의 7개 테이블은 모두 0건이다.
- 최신 상태를 `.build/live-ux/after.sqlite3`에 백업했다. `runtime.json`에 보존 검사·제공 파일 일치·네이티브 빌드 해시를 기록했다. 시험 자료와 산출물은 기존 ignore 대상이다.
- 8766이 제공하는 app.js/mic.js/app.css는 최종 디스크 내용과 일치한다. Python 서버 변경은 없어 재시작하지 않았다. 네이티브 브리지는 다시 빌드했으며 다음 마이크 시작부터 사용한다. 기존 탭이 저장 완료·마이크 미사용 상태인 것을 확인한 뒤 새로고침하고 문진 탭을 열었다.
- 시험 탭과 8768 서버를 종료하고 임시 viewport 설정을 해제했다. 기존 변경을 보존한 미커밋 상태이며 commit/push는 하지 않았다.

## N — macOS 네이티브 전환 / Xcode 27 업데이트 대기 (2026-09-17)

### N0 — 범위와 중단 상태

- 사용자 요청: 웹 UI를 SwiftUI/AppKit 기반 macOS 27 전용 앱으로 전환하고 Liquid Glass·Mac 기본 조작을 활용한다. 네이티브 앱/참조 자료 GUI 확인과 합성 DB 마이크 시험을 명시적으로 승인했다.
- 현재 구현: `macos/Sources/ClinicMac/`의 네이티브 창, 환자 목록, 단계별 문서, Inspector, 자동 저장/충돌 처리, AI 모델 선택/후보 비교·적용, 설문/검사 폼, 파일 패널, 키보드 명령, AVAudioEngine 마이크. 기존 Python/SQLite API를 재사용하며 웹뷰는 없다.
- 산출물: `.build/native/진료실.app`. 실행 설정은 합성 시험 DB `.build/native-qa/clinic.sqlite3`, 포트 8768. 앱 번들의 Runtime.json은 프로젝트/Python 경로에 의존한다. 독립 배포 앱·전체 기능 검증 완료로 판단하지 않는다.
- 사용자가 Xcode 27 업데이트 완료 후 재개하자고 요청하여 추가 구현/빌드를 중단했다. 소스·시험 DB·결과를 보존했다. 마이크와 시험 앱은 종료했고 시험 서버만 종료했다. 원래 8766 서버·진료 DB는 유지한다. commit/push 없음.

### N1 — 실제로 확인한 결과

- 설치 당시 전체 Xcode SDK는 26.5, 별도 CLT는 macOS 27 SDK/Swift 6.4였다. 명시적 CLT 경로로 arm64 macOS 27 Release `.app` 빌드·ad-hoc 서명·codesign 검증 성공. 최소 네이티브 창을 실행한 뒤 전체 UI를 구현했다.
- Apple 공식 macOS 27 디자인 리소스와 Sketch의 Sidebars 컴포넌트를 확인했다. Figma는 제공 URL의 페이지 제목까지 확인했다.
- 합성 환자 3명으로 실제 native UI 실행: 환자 선택, 문진/진료 전 단축키, 읽기 기본 문서, SOAP O 편집·저장, Inspector 열기/닫기 및 좁아진 본문의 세로 재배치 확인. SOAP O 저장 값은 별도 API 재조회로 확인했다.
- 네이티브 검사 폼에서 혈압 120/80, 체온 36.5를 입력·저장하고 진료 전 화면에 반영됨을 확인했다.
- 로컬 AFM 진료 전 요약 생성 → 후보 비교 → 한 항목 적용을 실제 GUI로 수행했다. API 재조회에서 a 본문 213자와 source provenance를 확인했다. 생성 내용에 부정 표현 오류(다리 저림 없음 → 다리 저림)가 있어 임상 정확성 통과로 판단하지 않는다.
- 마이크 첫 시험은 Swift 6 오디오 콜백이 MainActor로 추론돼 `_dispatch_assert_queue_fail`로 종료됐다. crash report의 `NativeMicrophone.start` callback을 확인했다. callback을 `@Sendable`로 명시하고 macOS 27 오류 반환형 public tap API의 Swift imported `__installTap(... error:())`를 적용했다. 재빌드 성공 후 실제 마이크가 1분 이상 유지됐고 Liquid Glass 상태 바·타이머·종료가 동작했다. 인식된 발화가 없는 상태로 종료했고 기존 전사는 보존됐다. 실제 발화→회색/확정 텍스트·입력 음량 반응은 아직 미검증이다.
- CLT SDK의 State macro plugin 누락으로 `@State` 컴파일 실패를 관측했다. 현재 `ViewState<Value> = SwiftUI.State<Value>` 공개 property wrapper 별칭을 사용한다. Xcode 27 설치 후 정식 도구 환경에서 재검토한다.
- Swift Testing 소스 `macos/Tests/ClinicMacTests.swift`를 추가했다. SwiftPM 실행은 test extension의 `init(dictionaryLiteral:)` 접근 수준 오류로 실패하여 테스트 본문은 실행되지 않았다. 로그 `.build/native-qa/swift-tests.log`. 이 시험을 통과로 표시하지 않는다. 기존 Python 회귀 65개는 이전 L 단계 결과이며 이번 네이티브 작업에서 재실행하지 않았다.

### N2 — 업데이트 후 첫 작업 / 남은 수용 기준

1. Xcode 27 설치 완료를 CLI로 확인하고 실제 SDK/Swift/first-launch 상태를 점검한다. 전역 xcode-select는 임의 변경하지 않고 프로젝트 명령에 DEVELOPER_DIR를 명시한다. 필요 시 사용자가 처리해야 하는 license/설치 프롬프트는 구분한다.
2. 정식 Xcode 27 경로로 빌드 스크립트·테스트를 정리하고 State 별칭/SDK workaround 필요성을 다시 확인한다. test JSON dictionary-literal conformance 접근 수준 오류부터 수정한다.
3. 원본 DB를 보존하며 합성 8768에서 자동 저장 경쟁/항목 충돌/복구/이력 복원, Cloud·Cloud Pro 후보, 새 접수/재진/안내, 녹음 파일 입력을 실제로 검증한다.
4. 마이크 실제 발화의 임시/확정 색상, 전사 보존, 장치·권한 실패, 종료/취소를 확인한다. 복구문 상태의 숨김/재시작 및 창 닫기 후 저장·마이크 lifecycle을 보완·검증한다.
5. 아이콘, String Catalog, 다크 모드/창 축소/키보드·VoiceOver, 완성 화면을 확인한다. 제공된 Xcode 27 업데이트 안내의 Preview variants/Device Hub, Foundation Models Instruments, Icon Composer, 외부 agent 지원은 설치 후 실제 사용 가능 범위를 확인한다.
6. PRD N1~N7은 일부 구현·검증 상태다. 전체 완성으로 선언하지 않는다. 사용자 기존 진료 자료로 전환·앱 설치·최종 런타임 검증은 아직 하지 않았다.

### N3 — Xcode 27 MCP 연결·빌드·Preview 실제 실행 (2026-09-17 08:29 KST)

- Xcode 27.0 / build 27A266a / Swift 6.4 / macOS 27 SDK를 실제 CLI로 확인했다. `scripts/build_macos.sh`는 CLT 고정 경로 대신 DEVELOPER_DIR와 xcrun을 사용하도록 변경했다. Release 빌드 성공.
- `mcpbridge` initialize는 응답했으나 초기 도구 목록은 시간 초과했다. `Package.swift`를 workspace로 열도록 요청한 것은 잘못된 경로였고 사용자 화면에 Swift Source cannot be opened outside of a workspace 오류를 유발했다. 정식 `macos/ClinicMac.xcodeproj`와 공유 ClinicMac scheme, My Mac 실행 대상으로 해결했다.
- XcodeOpenWorkspace를 통해 에이전트/프로젝트 폴더 접근 승인 후 실제 도구 조회·프로젝트 열기에 성공했다. 현재 세션은 `.build/native-qa/mcp_call.py`에서 Python JSON-RPC→`xcrun mcpbridge`를 사용하는 방식이다. 승인 대상은 해당 Python 클라이언트와 clinic-ai/macos 폴더이며 Codex 전역 MCP 설정을 변경하거나 전체 에이전트 자동 허용을 켜지 않았다.
- MCP BuildProject: The project built successfully, 6.378초, errors=[]를 반환했다. CLI xcodebuild도 성공했다. Xcode 프로젝트의 기본 runtime은 합성 시험 DB/8768이고 원본 8766은 유지한다. Runtime.json 생성 단계는 `scripts/native_runtime.py`를 사용하며 실제 데이터는 앱 번들에 복사하지 않는다.
- Swift Testing: dictionaryLiteral init 접근 수준 오류를 수정했다. 먼저 pure 2개 실행 및 integration 미실행을 확인했고, 이후 `CLINIC_NATIVE_INTEGRATION=1`로 8768 합성 서비스에 연결해 3개/0.053초 통과했다. 저장 중 추가 입력, 같은 항목 충돌 검출, 내 입력 선택·재저장, 이력 존재, 파일 기반 미저장 초안 복원·저장 후 제거, 기존 AI 적용 출처 확인을 포함한다. 로그 `.build/native-qa/xcode27-integration.log`. 앱 scheme의 Xcode Testables와 Swift package tests는 아직 통합하지 않았다.
- 실제 EncounterView를 사용하는 라이트/다크 #Preview를 추가했다. 처음 flat source references에서는 MCP가 파일을 찾지 못해 Sources group으로 정리하고 workspace를 다시 열었다. 최초 Preview 실행은 JIT symbol/linking 오류 및 NSBundle 예외로 종료됐다. PRODUCT_MODULE_NAME=ClinicMac을 명시한 뒤 라이트 Preview 생성에 성공했다. 다크 첫 반환 이미지는 빈 창이어서 성공으로 판정하지 않았고 재호출한 이미지에서 전체 본문·SOAP·색상을 확인했다.
- 최종 Preview 이미지: `.build/native-qa/preview-light.png`, `preview-dark.png`. Mac mini / macOS 27.0에서 MCP RenderPreview 반환 errors=[] 및 이미지 직접 확인. 미리보기는 합성 고정 입력으로 실제 UI component를 렌더하며 운영 서비스에 연결하지 않는다.
- MCP RunProject: processIdentifier 89121, The app was launched successfully, buildErrors=[] 확인. CUA로 해당 Xcode DerivedData 앱의 실제 창을 읽어 합성 환자 4개·저장된 복구 시험 내용·네이티브 툴바를 확인했다. 마이크는 켜지 않았다. 시험 앱과 8768 서비스는 다음 검증을 위해 실행 상태로 두었다.
- 이번 결과는 Xcode 외부 에이전트 연결·빌드·Preview·앱 실행 검증이다. N2에 기록한 실제 발화/파일 입력/나머지 업무/접근성·아이콘·localization·종료 복구 검증은 아직 남아 있다. 전체 네이티브 전환 완료 또는 임상 정확성 통과를 의미하지 않는다. commit/push 없음.


### N4 — 실제 마이크·파일·종료 복구 및 정식 Release 검증 (2026-09-17)

- 합성 전용 8768 DB에서 네이티브 새 환자 접수와 자동 방문 생성을 확인했다. 실제 마이크 입력에서 음량 피드백, 회색 임시 구간과 진한 확정 구간을 동시에 관찰했다. 약 3분 30초 후 종료했고 DB에 전사 120자/27개 시간 구간이 저장됐다. 시험 발화 원문은 이 문서/Git에 복제하지 않는다. 검은색은 ASR 확정이며 LLM 교정이 아니다.
- 마이크 진행 중 창 닫기 → 저장/계속 작업 경고 → 계속 작업 → 같은 창과 마이크 유지 → 종료·저장을 실제 GUI에서 확인했다. WindowCloseGuard가 종료 취소 시 창을 보존한다.
- 실패 전사 복구문을 앱 재실행 후에도 되찾도록 Application Support/ClinicAI/Recovery에 텍스트만 0600으로 저장한다. 복구를 문서의 durable draft로 옮긴 뒤에만 지운다. PCM은 AVAudioEngine→메모리→localhost→pipe 경로이고 오디오 파일 쓰기 코드를 추가하지 않았다. 강제 프로세스 종료 전 모든 실시간 부분 문장을 보존한다는 보장은 하지 않는다.
- 네이티브 NSOpenPanel과 ⇧⌘O로 기존 synthetic.aiff를 선택해 실제 Apple 파일 전사를 완료하고 저장된 본문을 확인했다. 경로 입력 과정에서 CUA AX 상태가 화면보다 늦게 갱신되는 문제를 겪었으나 최종 파일 선택/전사 성공을 확인했다. 원본 파일은 변경하지 않았다.
- ⌘4 단계 이동, 안내문/메시지 한국어 편집, ⌘S 저장, 안내 기록 로컬 저장을 확인했다. 메시지 미입력 오류와 입력 후 성공 표시를 각각 관찰했다. 외부 발송 없음.
- 이력 미선택 때 빈 값을 복구할 수 있던 버튼을 숨기고 선택 응답의 역전도 차단했다. select의 빈 선택은 서버 계약대로 빈 문자열, multi_select는 문자열 배열로 전달하도록 수정했다. 후보 목록은 변하는 전체 JSON 대신 ID로 식별한다. tap 설치 실패 시 removeTap을 호출하지 않는다.
- AppKit로 만든 앱 아이콘과 한국어 String Catalog를 CLI/Xcode 번들 모두에 추가했다. Icon Composer의 다층 아이콘을 구현했다고 주장하지 않는다. 녹음/텍스트 가져오기는 파일 메뉴와 단축키로도 제공한다.
- 최종 `scripts/build_macos.sh` Release/ad-hoc codesign 검증 성공. Xcode 27 Release `xcodebuild ... -configuration Release ... build` 성공. Swift Testing 4개 통과(0.081초): JSON/후보 계약, 전사 복구문 재실행, 실제 API 저장 중 추가 입력·충돌·초안 복구·출처. Python 회귀 65개 통과(8.748초). 로그는 `.build/native-qa/final-build.log`, `xcode-release.log`, `final-tests.log`, `python-tests.log`, 요약 `result.json`.
- 네이티브 화면에서 긴 합성 파일 전사의 SOAP 생성을 요청했으나 로컬 모델이 생성 실패를 반환했다. 선택한 Cloud Pro를 명시적으로 실행한 결과 앱이 시작한 서버에서 `연결된 단축어 설정을 확인하지 못했습니다`로 실패했다. CLI의 같은 두 workflow digest는 설정 해시와 일치한다. 원인은 아직 확정하지 않았으며 권한 문제라고 단정하지 않는다. 자동 fallback 없음, 실패한 후보가 기존 SOAP를 덮어쓰지 않음. 이 실행 경계 차이가 해소될 때까지 N4 AI의 네이티브 전체 통과를 선언하지 않는다.
- 기존 `.build/qa-case/clinic.sqlite3`의 7개 업무 테이블 행 수는 1/1/1/0/1/0/0, 기본 `data/clinic.sqlite3`는 모두 0, 두 DB integrity_check=ok. 이번 시험은 native-qa DB에서만 수행했다. 현재 앱은 QA/8768이며 원본 DB 전환·Applications 설치는 아직 하지 않았다.
- 현재 판정: N1 빌드·실행 통과, N2 네이티브 UI/실제 Glass 및 light/dark preview 확인, N3 자동 저장/충돌/텍스트 복구 통과(이력 복구 GUI 재시험 남음), N4 native AI 실패 원인 미해결, N5 실제 마이크·파일 전사 통과, N6 접수/검사/방문/안내 주요 흐름 확인, N7 종료 취소·단축키 확인(VoiceOver-only/축소 창 전체 업무 미완료). 사용자 기존 변경 보존, commit/push 없음. 전체 100% 완료 또는 배포 준비 완료로 해석하지 않는다.

- N4 추가 진단: 설정 읽기 실패에 예외 종류/SQLite 코드만 표시하도록 보완(자료 내용·자격증명 노출 없음). 앱/소유 서버를 정상 종료·재시작하고 같은 합성 요청을 실행하자 `OperationalError: SQLITE_CANTOPEN`이 확인됐다. 기존 CLI의 workflow 검증은 두 모델 모두 일치한다. 개인정보 보호 설정 화면 확인은 기존 승인과 다른 앱 범위이므로 별도 읽기 승인을 요청했다. 검증을 제거하거나 다른 실행 주체로 우회하지 않았다.

- 편집 이력 추가 GUI 시험: 미선택 상태에서는 `복구할 이력을 선택하세요`만 표시, 실제 버전 선택 뒤 SOAP S의 빈 항목 복구를 실행했다. SQLite 재조회로 `soap_s == ""`와 SOAP 네 구간의 빈 값 재구성을 확인했다. N3의 이력 복구 GUI 잔여는 해소됐다. 오류 메시지 보완 후 AI 모델 집중 테스트 9개 통과(`ai-error-tests.log`). 마지막 화면은 합성 김민준 환자의 문진/SOAP 화면이며 마이크는 종료 상태다. 개인정보 보호 화면 승인 응답 대기 중이다.

- 개인정보 보호 설정 읽기 승인 후 실제 System Settings GUI 확인: 파일 및 폴더 목록에는 진료실이 없고, 전체 디스크 접근 권한에는 진료실 항목이 존재하며 스위치가 off였다. 권한은 변경하지 않았다. 이 상태는 native SQLITE_CANTOPEN/CLI 성공 차이의 원인 후보를 뒷받침하지만, 허용 후 재시험 전에는 인과관계나 Cloud 성공을 확정하지 않는다. 전체 디스크 접근은 단축어 DB에 한정되지 않으므로 변경에는 별도 명시 승인이 필요하다.


### N5 — 사용자 권한 허용 후 Cloud 재시험 (2026-09-17 09:27 KST)

- 사용자가 전체 디스크 접근 권한을 직접 켰다고 알렸다. 실행 중이던 Xcode DerivedData 앱과 그 자식 Python 서버를 정상 종료하고 8768 listener가 사라진 것을 확인했다. `.build/native/진료실.app`을 다시 실행하여 이 앱(PID 99630)이 시작한 서버(PID 99634, 합성 native-qa DB/8768)에서 시험했다. CLI에서 별도 서버를 띄워 권한 경계를 우회하지 않았다.
- 이전 실패와 같은 합성 파일 전사 방문/단계 b를 Cloud Pro와 Cloud로 명시 실행했다. Cloud Pro 4.0초, Cloud 6.0초에 completed, error 빈 값, content/soap/generation 반환. 두 생성 모두 기존 문서가 그대로 보존됐고 자동 적용/자동 fallback 없음. `.build/native-qa/cloud-permission-retest.json`. 권한 허용·재시작 후 SQLITE_CANTOPEN 문제는 재현되지 않았다.
- 실제 네이티브 후보 비교 화면에 두 모델의 `후보 준비`가 표시됐다. Cloud Pro를 펼쳐 기존/새 후보를 비교하고 `환자 진술(S)` 한 항목만 적용했다. UI 성공 메시지와 DB 재조회 모두 확인: source_job 일치, generation.id=cloud-pro, S 비어 있지 않음, O/A/P 비어 있음 유지, 이력 10개. `.build/native-qa/cloud-apply-retest.json`. 시험 자료는 합성이며 사용자 실제 진료 자료를 전송하지 않았다.
- 네이티브 Cloud/Cloud Pro 실행 경계 및 부분 적용 검증은 통과. N4의 로컬 긴 전사 생성 실패는 별개이며 이번 결과로 해결됐다고 표시하지 않는다. 임상 내용 정확도·전체 VoiceOver/축소 창 검증과 실제 DB 전환/설치는 별도 잔여다. 코드 변경·빌드·commit/push 없음. 기존 PRD의 AI 실패 상태를 이번 검증 결과에 맞게 수정했다.


## Q1 — AFM 3 SOAP 충실도 비교 (2026-09-17)

- 요청: 로컬 2종/Cloud 2종을 비교하고 이전 Cloud Pro의 누락·의미 확대·처방 의미 변경을 평가. 운영 기본 프롬프트와 실제 진료 DB는 바꾸지 않았다.
- 모델 확인: 공개 macOS 27 SDK에 core3/coreAdvanced3 Variant 표시는 있지만 variant 지정 생성자는 찾지 못했다. 실제 default/general/contentTagging 조회는 모두 AFM 3 Core Advanced / available / 8192. Core를 별도 모델로 호출한 증거가 없어 미시험으로 분류했다. Apple 직원의 장치별 모델 설명: https://developer.apple.com/forums/thread/832910 ; API: https://developer.apple.com/documentation/foundationmodels/systemlanguagemodel/variant-swift.struct . 다른 useCase를 Core라고 부르거나 중복 집계하지 않았다.
- 실행: `python3 -m scripts.evaluate_soap_quality --run`. 정확한 합성 대본(gold)과 기존 실제 Apple ASR(asr), 기존 프롬프트 1회와 fidelity-v1 2회씩 × 접근 가능한 3모델 = 18회. 18회 모두 구조화 결과 반환. 임상 품질 통과와 구분한다. 로컬 시험 bridge는 기존 Swift source의 시험 사본으로만 상세 오류 진단을 추가했으며 production bridge를 교체하지 않았다.
- 공정성: 동일 version/lane별 공통 요청 hash 1개씩 확인. transport는 로컬 Swift DynamicGenerationSchema/temperature 0.2와 Cloud Shortcuts JSON 계약으로 다르다. Cloud 샘플링 설정은 제어하지 않았고, 로컬과 Cloud를 독립 서비스로 동시에 진행했다. 따라서 순수 모델 단독 성능 벤치마크가 아니라 이 앱의 연결 경로 비교다.
- 평가: Codex가 원 대본·실제 모델 입력·SOAP와 content를 직접 대조했다. 현재 부정 항목, 한정어/시간, 수치, 미검사, 진단/처방 미정, S/O/A/P 배치, 모호한 전사 표시, 근거 없는 추가와 지침 유출을 점검했다. ASR가 소실한 사실과 LLM이 새로 바꾼 사실을 구분했다. 독립 임상의의 검증은 아니다.

| 모델 | 실행 | 보강 gold 2회 | 보강 ASR 2회 | 주요 관측 |
| --- | --- | --- | --- | --- |
| AFM 3 Core | 미시험 | — | — | 이 Mac에서 별도 선택/호출 경로 미확인 |
| AFM 3 Core Advanced | 6/6 생성, 평균 25.0초 | 두 회 모두 문서 품질 실패 | 두 회 모두 실패 | 혈압 74/116 역전, 원문/지침 반복, 마지막 ASR 반복에서 부정→긍정 역전 |
| AFM 3 Cloud | 6/6 생성, 평균 5.3초 | 두 회 모두 실패 | 두 회 모두 실패 | 처방 미정과 한약 처방 없음 동시 출력, 모두 없어요의 대상 누락 |
| AFM 3 Cloud Pro | 6/6 생성, 평균 6.5초 | 이번 사례 주요 충실도 기준 2/2 충족 | 두 회 모두 실패 | 1회는 SOAP에서 의미 확인 필요지만 content에서 약침 단정, 2회는 SOAP도 약침 단정 |

- baseline Cloud Pro는 정확한 대본에서도 큰 외상 없음→외상력 없음, 약·침 처방 미정→없음으로 바꿨다. fidelity-v1은 이 부분과 발열 부정 보존을 개선했지만 ASR의 애매한 용어를 모든 출력 필드에서 일관되게 보존하지 못했다.
- 현재 판정: 이번 합성 사례에서는 Cloud Pro가 가장 유망하지만 전체 전사→SOAP 흐름의 품질 합격 모델은 없다. Core Advanced의 과거 생성 실패와 달리 이번 6회는 생성됐으며 생성 가능성과 품질이 다름을 확인했다. 1개 사례/2입력 형태/소수 반복이므로 범용 정확도·우열로 일반화하지 않는다.
- 산출물: `.build/soap-quality/{cases,results,review,run-config}.json`, `variants.txt`, `progress.log`; Git에는 시험 코드만 추가하고 원문·응답은 기존 .build 제외 범위에 둔다. 항목별 판정은 review.json. 코드 py_compile 및 git diff --check 검증. commit/push 없음.
- 다음 개선 기준: 약·침/약침 같은 애매한 입력을 모든 필드에서 원문+확인 필요로 유지, 단계 b에서 guide/rx_guide 등 무관 지침 분리, 사실 추출과 SOAP 구성 분리 실험, 숫자·부정 표현 대조. 다른 합성 사례와 반복에서 검증한 후에만 기본 프롬프트 승격. 단순 지침 추가가 모든 모델을 개선한다는 가정은 폐기한다.

## Q2 / 분할 생성 loop 1 — 계약 불일치 (2026-09-17)

- PRD에 승인 계획과 종료 판정 기준을 기록했다. 원문 묶음→사실 추출→S/O/A/P 개별 작성→코드 조립을 구현하고 시험 전 기본 생성 경로는 유지했다.
- 집중 테스트 6개 통과: 문답 보존, 과대 문단 무단 절단 방지, 매 단계 원문 제공, 중간 실패, 추출 형식, 저장된 지침 고정.
- 실제 3모델×gold/ASR 6회 모두 사실 추출 계약 단계에서 실패했다. Cloud 한 줄 문자열 계약과 추출 본문의 줄 시작 S/O/A/P 검사 충돌을 발견했다. 로컬도 같은 자유 본문 제목 검사를 만족하지 못했으며 내용 품질 판정까지 도달하지 않았다. 산출물 `.build/soap-split/loop1.json` 및 `.build/soap-quality/split-progress.log`.
- 회고: 자유 본문에 별도 파서를 얹는 대신 이미 지원되는 네 문자열 필드 스키마를 추출 단계에도 사용한다. 형식 검사를 제거해서 통과시키지 않는다. 앱 기본 경로·DB 변경 없음.

## Q3 / 분할 생성 loop 2 — 구조화 추출과 의미 대조 (2026-09-17)

- 추출 단계를 기존 네 필드 스키마로 변경. 동일 gold/ASR×3모델 실행. 원문+전체 추출 메모를 각 항목 작성에 제공했다. 산출물 `.build/soap-split/loop2.json`, 단계별 trace.jsonl.
- Core Advanced gold 29.13초/ASR 114.25초 생성. gold는 S에 진찰/처방이 섞이고 O에서 혈압 누락, ASR는 장황한 원문 반복·불명확 용어 무표시. 품질 실패.
- Cloud는 gold 8.00초/ASR 7.31초에 S 응답 본문 계약 실패. 형식 검사를 유지했고 SOAP 후보를 반환하지 않았다. 실패 원시 응답은 이 회차에 보존하지 않아 구체 필드 유형은 미확인이다.
- Cloud Pro gold 10.91초는 이번 사례 핵심 충실도 충족. ASR 11.59초는 약 침 의미 확인을 보존했지만, 저림/대소변/혈압을 추정 교정하고 기립근 압통·반사에 확인 필요를 일관되게 붙이지 않아 실패.
- 회고: 전체 추출 메모가 항목 간 내용 혼입을 유발할 수 있으므로 해당 항목 메모만 제공한다. 원문은 계속 제공한다. 단일 content 필드의 의미를 Swift schema에도 명시하고 Cloud 지침에도 content 키를 재강조한다. 합성 holdout 사례를 추가한다. 71개 Python 회귀 통과는 임상 품질 통과와 구분한다.

## Q4 / 분할 생성 loop 3 — 항목별 메모 및 holdout (2026-09-17)

- 해당 항목 메모만 제공하고 Swift contentDescription을 추가했다. content 키 보존을 명시하고 합성 호흡기 문답 holdout을 추가했다. 3모델×3입력 9회 생성. `.build/soap-split/loop3.json`, 원시 응답/요청 raw.jsonl, 단계별 trace.jsonl.
- Core Advanced gold/asr의 S가 문자 `{` 하나였다. 나머지 필드가 있어 단순 문자열 검사는 통과했지만 의미 있는 본문이 아니다. holdout에는 '전반적으로 모든 증상 없음'으로 양성 증상을 부정하고 계획이 누락됐다. 전부 품질 실패.
- Cloud는 세 입력 모두 항목 혼입과 원문 없는 정보가 남았다. holdout의 복용약 없음은 원문에 없다. Cloud Pro도 gold O에 S/P 혼입, ASR에서는 수치·용어를 교정, holdout A에 P 혼입. 엄격한 완료 기준 미달. 회차 3의 독해 결과로 분할이 단일보다 항상 낫다는 가설을 기각한다.
- 앱에 시험 메뉴와 strategy 큐 보존/동일 방식 재시도/중간 실패 보존을 연결했다. 집중 Python 27개 통과, 네이티브 Release 빌드·서명 통과. 앱 정상 종료 후 재실행, 실제 GUI에서 기존 방식/분할 시험 메뉴 확인. 아직 새 메뉴의 모델 실행 결과 검증 전.
- 회고: Cloud 출력 계약의 content 설명이 여전히 '검토용 요약'이라 항목 제한과 충돌할 수 있다. Swift 설명도 출력 형식 지시를 섞지 않고 항목 자체 설명으로 제한한다. 다음 회차는 두 transport의 필드 설명을 맞추고 구두점만 있는 응답을 거부한다. 형식 거부를 임상 합격으로 계산하지 않는다.

## Q5 / 분할 생성 loop 4 — 출력 필드 정렬·반복·네이티브 검증 (2026-09-17)

- 두 transport에 항목별 content 설명을 전달하고 구두점뿐인 응답을 거부했다. 선택한 모델 고정, 원문/지침/분할 큐 스냅샷, 5회 호출 추적, 기존 단일 경로 유지, 동일 방식 재시도와 네이티브 시험 메뉴를 구현했다. 추가 CDSS는 이번 분할 경로에서 생성하지 않고 정적 검토 안내를 반환한다.
- 실행: `python3 -m scripts.evaluate_soap_split --run --bridge .build/soap-split/bridge-v3 --cases .build/soap-split/cases.json --lanes gold asr holdout --output .build/soap-split/loop4.json`. 9회 중 8회 생성. Core Advanced ASR는 S의 불완전 본문 검사에서 실패했다. Cloud Pro 추가 6회 반복은 5회 생성, holdout 마지막 A 호출은 단축어 실행 실패. `.build/soap-split/loop4-repeat.json`.
- 판정: Core Advanced는 지침 노출/부정 대상 누락/계획 누락, Cloud는 S/O/A/P 혼입이 남아 품질 실패. Cloud Pro gold 첫 회는 '밤에 깨는 통증'을 '야간통'으로 넓혔고 추가 2회는 주요 사실을 보존했다. holdout 성공 2회는 시점·알레르기·미측정·치료 미정을 보존했다. ASR 3회 모두 혈압/용어/약 침의 불확실성 표시가 일관되지 않아 실패. 형식과 임상 품질을 구분한다.
- 최종 버전 성공 호출 평균: Core Advanced 13.07초(2/3 생성), Cloud 14.64초(3/3), Cloud Pro 11.03초(8/9). 실패 시간은 평균에서 제외했다. 실제 모델 단계는 한 묶음당 5회. 성공한 local gold 각 입력 758/882/882/821/859토큰, 합계 4,202토큰. 이전 단일 gold 1,053토큰과 비교해 호출당 입력은 줄었지만 총 입력량은 늘었다. Cloud 토큰은 미제공이며 모델의 자기 보고를 사용하지 않는다.
- 단일 fidelity-v1의 동시점 재비교 3회는 모두 Shortcuts 실행 실패(약 0.22초)라 새 품질 비교를 만들지 못했다. 기존 Q1의 단일 생성 결과는 역사적 비교로만 남긴다. 반복 실패를 자동 재요청하거나 다른 모델로 전환하지 않았다. `shortcuts list` 읽기는 성공했으나 실행 실패의 정확한 원인은 확정하지 못했다.
- 누적: 분할 loop **4회**, CLI 분할 시나리오 36회(6+6+9+9+6 반복), 추가 단일 재비교 3회 = **39회 시도**. 세부 모델 호출 수와 문서 생성 시도 수를 혼동하지 않는다. 기준을 충족할 때까지 개선한다는 목표는 임상 품질에서 미달이며, 4회 통과나 최적성 달성으로 표시하지 않는다.
- Python 전체 회귀 77개 통과(8.702초; 기존 테스트 경로에서 sqlite ResourceWarning 1개 출력), Swift CLI Release 빌드/ad-hoc 서명 검증 통과. `final-tests.log`, `native-final-build.log`. 새로운 Swift Testing 전체를 이번 회차에 다시 실행하지는 않았다.
- 실제 앱 QA: `.build/native/진료실.app`이 시작한 8768/native-qa 서버에서 새 합성 방문 생성 전 SQLite 백업을 만들었다. 실제 GUI에서 분할 메뉴→Core Advanced 5단계→후보 준비→O 항목만 적용을 확인했다. S의 '기존 편집 보존 시험'과 generation.soap_strategy=split, prompt_version=soap-split-v4, job_id 일치를 API로 재확인했다. 최초 감사 스크립트가 실제 계약 job_id 대신 source_job을 조회해 KeyError를 냈고 조회 키를 고쳐 검증했다(제품 오류 아님). `.build/soap-split/native-{context,jobs,apply}.json`.
- 네이티브 Cloud Pro는 사실 단계 전 SQLITE_CANTOPEN으로 실패했다. 새 빌드에서 이전 N5의 권한 상태를 재사용할 수 없었으며 UI/DB 모두 기존 문서 보존을 확인했다. 사용자가 System Settings 읽기를 승인한 뒤 전체 디스크 접근의 진료실 스위치가 **off**임을 AX+화면으로 확인했다. 권한은 변경하지 않았다. 진료실 권한 켜기/재시작/합성 재시험을 별도 요청했고 승인 응답 대기 중이다. ad-hoc 재빌드와 권한 상태 변화의 인과관계는 확정하지 않았다.
- 데이터: 기본 DB 7개 테이블 모두 0, 원본 qa-case 1/1/1/0/1/0/0 유지, 세 DB integrity_check=ok. native-qa 기존 업무 행은 시험 전 백업과 모두 일치하며 새 합성 방문에만 후보와 부분 적용을 수행했다. `.build/soap-split/data-check.json`. commit/push 없음.
- 회고/최선 판정: **이 분할 방식이 최선이라는 근거가 없다.** 단순 분할로 작은 모델의 의미 보존 능력과 ASR 불명확성을 해결하지 못했고 지연/총 호출량이 증가했다. 최종 방식을 기본값으로 승격하지 않는다. 실패를 숨기거나 원문을 자동 교정해 합격시키지 않는다. 다음 품질 실험은 더 많은 프롬프트 문구 추가보다 근거 구간을 검증하는 사실 추출과 실제 ASR 문답/모호성 처리에 초점을 맞춰야 한다. 현재 100% 완료를 선언하지 않는다.

### Q5 권한 후속 — 사용자 명시 승인 후 재시험 (2026-09-17)

- 사용자 지시로 System Settings의 진료실 전체 디스크 접근만 off→on으로 변경하고 on 상태를 확인했다. 다른 앱의 권한은 변경하지 않았다. 시스템의 종료 및 다시 열기로 기존 프로세스가 종료되지 않아 진료실을 정상 종료하고 8768 listener 소멸을 확인한 뒤 같은 .app을 다시 실행했다.
- 앱이 시작한 서버에 동일 합성 방문의 Cloud Pro 분할 생성을 요청했다. SQLITE_CANTOPEN은 재현되지 않았고 단축어 실행 단계까지 진행했으나, 1/사실 단계에서 단축어 실행 실패를 반환했다(약 1.03초). 기존 문서는 그대로 보존됐다. `.build/soap-split/permission-retest.json`. 권한 켜기와 DB 접근 문제 해소를 Cloud 생성 성공으로 대신하지 않는다. 코드/프롬프트 변경이 없는 권한 재시험이므로 분할 설계 loop 수는 4회로 유지한다.

## S1 — 개발자 서명 고정 및 재빌드 권한 유지 검증 (2026-09-17)

- 사용자 승인: 같은 개발자 인증서로 서명 전환 및 해당 업무의 GUI 조작. 유효한 Apple Development 인증서(개인 식별값은 공개본에서 제거)를 확인했다. 비밀키를 추출하거나 새 인증서를 발급하지 않았다.
- CLI build_macos.sh 및 Xcode Debug/Release를 같은 인증서 SHA-1과 com.raphael.ClinicAI로 고정했다. 인증서가 없으면 명시적으로 실패하며 ad-hoc fallback은 없다. CLI 서명과 Xcode Release 빌드/서명 모두 성공했다.
- 초기 ad-hoc DR은 cdhash 기반이었다. 변경 후 CLI/Xcode DR은 identifier + Apple anchor + 동일 인증서 조건이며 정확히 일치했다. 두 빌드의 CDHash는 달랐다. `.build/signing-qa/comparison.json`. 최초 비교 스크립트가 DR을 stderr에서만 읽어 실패하여 stdout+stderr로 수정했다(제품 오류 아님).
- 새 인증서로 첫 실행 시 기존 권한이 적용되지 않아 SQLITE_CANTOPEN 재현. System Settings 화면에 남아 있던 표시와 실제 접근을 구분했고 진료실 스위치를 조작해 새 서명으로 권한을 재연결했다. 앱을 정상 종료하고 listener 소멸 후 재실행하자 DB 접근을 통과했다. 다른 앱의 권한은 변경하지 않았다.
- CLI 빌드를 `.build/signing-qa/cli-build.app`에 보존하고, 해시가 다른 Xcode Release 앱을 같은 `.build/native/진료실.app` 위치로 교체했다. 권한을 다시 조작하지 않고 실행해 단축어 DB 접근을 통과했고 System Settings 화면에서도 진료실 on을 확인했다. 동일 서명 조건의 다른 빌드에서 권한 유지 검증 통과. 현재 실행 앱은 이 Xcode Release 빌드, native-qa DB/8768이다.
- 별도 Cloud Pro 실패 원인 확인: 합성 1+1 요청의 shortcuts stderr가 '이 모델의 사용 한도에 도달했습니다. 나중에 다시 시도하십시오.'를 반환했다. `.build/signing-qa/shortcut-diagnostic.json`. 이후 ai_models.py가 알려진 한도 오류를 429/명확한 안내로 변환하게 수정했다. 원시 stderr는 제품 화면/로그에 그대로 노출하지 않는다. 자동 fallback/자동 재시도 없음.
- 마지막 앱 소유 서버의 합성 SOAP 분할 요청은 권한 오류 없이 사용 한도 오류를 반환했고 기존 문서가 보존됐다. 실제 후보 GUI에서도 사용 한도 안내를 확인했다. Cloud Pro 생성 성공은 사용 한도로 미확인이다. `.build/signing-qa/final-runtime.json`.
- 집중 테스트 40개 및 전체 회귀 78개 통과. 전체 회귀 로그는 `.build/signing-qa/all-tests.log`, Xcode 로그는 xcode.log. 집중 시험에서 sqlite ResourceWarning이 출력됐으며 이번 서명/오류 분류 수정과 별개로 보존했다. sh -n 및 git diff --check 통과. 원본 DB/개인 자료 변경·외부 배포·commit/push 없음.
- 서명 변경은 별도 검증 S1이며 SOAP 품질 loop는 기존 4회로 유지한다. 인증서 갱신/교체 시 서명 조건과 권한 유지를 다시 검증해야 한다. 개발 서명은 배포용 공증을 의미하지 않는다.

## S2 — Cloud/Cloud Pro 한도 비교 및 Apple 공식 문서 (2026-09-17)

- 사용자 요청에 따라 같은 합성 산술 입력으로 모델별 1회씩 CLI 호출했다. 연결된 workflow digest를 먼저 검증했다. 10:46:00 KST Cloud는 1.690초에 content=2/exit 0, 10:46:38 Cloud Pro는 0.221초에 사용 한도 도달/exit 1을 반환했다. `.build/signing-qa/cloud-limit-check.json`, `cloud-pro-limit-check.json`. 실제 진료 자료 전송·업무 DB 수정·설정 변경 없음.
- 판정: Pro 한도 도달이 현재 Cloud 호출까지 일괄 차단하지 않는다. 완전히 독립된 quota인지, 공통 상한과 모델별 상한이 함께 있는지는 이 실험으로 확정할 수 없다. 단순 산술 성공은 SOAP 품질 통과가 아니다.
- Apple 지원 https://support.apple.com/ko-kr/127901 (게시 2026-09-09)는 두 모델을 제한 대상에 포함하고 기능·요청 복잡성·시스템 수요·정책 등에 따라 달라지며 일정 시간 후 재사용 가능하다고 설명한다. 총량·잔여량·정확한 초기화 시각·두 모델의 공유 여부는 명시하지 않는다. 2027 소프트웨어/향후 적용이라는 문구가 있어 현 기기의 제한 구현과 동일 정책이라고 단정하지 않는다.
- macOS 27 메모 공식 설명 https://support.apple.com/en-by/guide/notes/not9474646a9/mac 각주는 단축어 AFM 3 Cloud 모델 등의 daily usage limits를 명시한다. 따라서 일일 한도 안내 자체는 공식 문서로 확인됐지만 이번 Pro 오류가 일일 소진인지 단기 제한인지는 미확인이다. 유료 확대는 향후 제공 설명이며 지금 구매 가능한 상품·가격은 확인하지 않았다.
- PCC 개발자 문서 https://developer.apple.com/private-cloud-compute/ 의 무료 API 사용 자격은 사용자 한도 무제한을 뜻하지 않는다. 추가 반복 호출 없이 조사 종료. 제품 코드 변경 없음, 문서 diff 검사만 수행. SOAP 품질 loop는 4회 유지.

## STT-1 — Notion AI와 진료실.app 동일 WAV 업로드 비교 (2026-09-17)

- 사용자 명시 승인: Computer Use, 진료세션 페이지의 Notion AI 전사와 진료실.app에 같은 파일을 업로드해 비교. 기존 85.668707초 합성 Yuna 음원 synthetic.aiff를 무손실 PCM WAV로 변환했다. 두 제품 모두 `.build/stt-comparison/same-input.wav` 선택. SHA-256 `7f9096037b0aa164e4223386118d30f6f6cfb155b21e3a63a06a4c461d87ebdf`; 진료실 저장 provenance도 일치. 실제 환자 음성은 전송하지 않았다.
- Notion: 기존 교육용 합성 진료세션 `[비공개 식별자 제거]` 하단에 새 비교 설명과 AI 노트 블록을 추가했다. 블록 `[비공개 식별자 제거]`, 제목 `STT 동일 음원 비교 · 합성 시험 · 2026-09-17`. ego-browser space 1에서 실제 파일 선택/업로드/받아쓰기 완료를 확인하고 DOM의 새 블록 전사만 추출했다. 기존 노트/요약/속성은 시험 대상으로 덮어쓰지 않았다. 삽입 중 마지막 기존 문장에 시험 제목이 붙은 것을 발견하여 원래 문장으로 복원하고 화면으로 확인했다. 자동 생성 요약은 평가에 사용하지 않았다.
- 진료실: 실제 네이티브 새 접수→문진→녹음 파일 가져오기→동일 WAV 선택→전사 완료 확인. native-qa/8768의 새 합성 세션 `[비공개 식별자 제거]`에 저장. 기존 DB SQLite 사본을 먼저 보존했으며 기존 7개 업무 테이블 행 모두 동일, integrity_check=ok. 시험 식별값만 사용했다. 앱/브리지 코드 수정, AFM 호출, 마이크 녹음 없음.
- 결과: Notion은 저림/외상/기립근/대소변장애를 보존했고 Apple은 절임/의상/기립군/대소변장에로 전사했다. 반면 Apple은 `3일 뒤`를 보존하고 Notion은 `3일 귀`로 전사했다. 둘 다 소변→소견, 압통→앞통, 심부건반사→신부건 판사, 요통→유통 오류. 발화된 의사 표기도 둘 다 잘못 인식했다(단일 TTS이므로 화자 분리 시험은 아님). NRS 4/10, 맥박 70, 체온 36.6, SLR 양측 음성, 진단/처방 미정 표현은 양쪽에 남았다. 혈압은 둘 다 `74분의 116`으로 표시하므로 임상 수치 정규화 정답으로 확정하지 않는다.
- 대본 대비 문자 편집률: NFC/소문자/문장부호·공백 제거 후 기준 413자, Apple 23편집=5.57%, Notion 19편집=4.60%. 차이는 4편집이다. 합성 입력 대본을 기준으로 했고 실제 오디오를 독립 청취해 정답 전사를 만들지 않았다. 숫자 구어 표현 차이도 포함되므로 이를 실음성 CER/정확도 또는 일반적 성능 수치로 부르지 않는다.
- 속도: 진료실 열기 클릭 시각 02:38:38.121Z→전사 저장 02:38:40.104076Z, 1.983초(단발 앱 작업 구간). Notion은 업로드 시작만 기록하고 완료를 연속 계측하지 않아 정확한 소요 시간/속도 우열을 보고하지 않는다. 파일 업로드 시험이므로 실시간 첫 글자/확정 지연도 평가하지 않았다.
- 판정: 이 음원에서는 Notion이 일부 용어와 대본 문자 일치에서 소폭 우세. 두 제품 모두 핵심 전문용어 오류가 남아 무검토 진료 기록으로 쓰기 어렵다. Notion의 UI/요약 완성도를 원시 STT 정확도 우위로 대체하지 않는다. 단일 음원·단일 합성 화자·각 1회로 실진료/장시간/중첩 발화 우열을 일반화할 수 없다.
- 증거: `.build/stt-comparison/{same-input.wav,reference-script.txt,apple-result.json,apple-transcript.txt,notion-result.json,notion-transcript.txt,metrics.json,preservation.json,before.sqlite3}`. 원문/오디오/DB는 Git 제외. 작업 코드는 바꾸지 않았으며 문서 diff 검사만 수행했다.

## STT-2 — 두 화자 실시간 마이크 비교 준비 (2026-09-17)

- 요청: 파일 업로드 대신 음원을 마이크로 입력하여 Notion/진료실의 화자 분리 및 실시간 표시 지연 비교. 같은 스피커→물리 마이크 경로에서 같은 음원을 사용한다. 입력된 마이크 오디오는 파일로 저장하지 않는다.
- 현재 `system_profiler SPAudioDataType -json`에서 Mac mini 스피커와 가상 ZoomAudioDevice만 검출됐다. 물리 입력 마이크/기본 입력 장치가 확인되지 않아 음원 재생 및 마이크 전사를 시작하지 않았다. USB/웹캠 마이크 연결을 사용자에게 요청했다. 가상 오디오 라우팅이나 시스템 설정을 임의 변경하지 않았다.
- 준비: Eddy 한국어/Yuna 두 합성 목소리가 A/B/A/B/A/B로 발화하는 39.635초 WAV. 대본에 의사·환자 표기를 말하지 않고 발화 사이 2초 휴지. `.build/stt-live-comparison/two-speakers.wav`, `manifest.json`에 각 발화 시작/끝과 대본 기록. SHA-256 ab8a502ce512583aaa97c3e045a3982281dad0355b0e22acedce058fe5aab38b. 이 파일은 합성 재생 자극이며 마이크 녹음이 아니다.
- 완료 기준: 물리 마이크의 입력 확인, 동일 음원 두 앱 시험, 발화 시작 대비 첫 글자/확정 표시 시각과 관측 오차 기록, 6개 발화의 화자 일관성 및 5개 전환 판정, 두 앱 마이크 종료 확인. 화자명 의미 추정과 음성 화자 분리를 구분한다. 짧은 합성 음원 결과를 실진료 대화 품질로 일반화하지 않는다.
- 현재는 준비만 완료했으며 실제 비교 결과는 없음. 앱 코드/기존 데이터/Notion 페이지 변경 없음.

### STT-2 입력 경로 변경 — 가상 마이크 설치 진행

- 사용자가 USB 웹캠 연결 해제 상태를 확인했고, 물리 스피커→마이크와 조건이 달라도 화자 분리/실시간 지연 비교를 진행하도록 요청했다. 이어 가상 마이크 설치를 명시 승인했다. 시험 입력을 동일 합성 WAV→가상 루프백→각 앱의 실제 마이크 경로로 변경한다. 물리 마이크/실내 소음 성능으로 일반화하지 않는다.
- 기존 ZoomAudioDevice는 CoreAudio에 보이지만 기본 입력 가능 속성이 0이고 시스템 사운드 입력 목록에도 나타나지 않았다. 직접 루프백 사용 가능성을 확인한 것으로 보고하지 않는다.
- Homebrew 공식 cask의 BlackHole 2ch 0.7.1 패키지 다운로드 완료. CLI 설치는 관리자 인증용 터미널 부재로 실패했고 공식 패키지를 Finder에서 열었다. 설치 프로그램의 사용권 계약 동의 단계에서 사용자 확인 대기. 아직 설치 성공/장치 인식/전사 시험 결과는 없다. 설치 안내의 재시작 요구를 확인했으며 임의 재시작하지 않았다.
- 후속: 사용자 승인 후 사용권 계약에 동의하고 설치를 실행했으나 관리자 인증 대기에서 사용자가 외출 중임을 알리고 중단을 지시했다. 그 시점에 작업을 중단했다. 설치 완료와 가상 마이크 인식은 미확인이고 실시간 비교는 시작하지 않았다. 자동 재개나 시스템 재시작을 예약하지 않았다.

## G1 — 네이티브 MVP 작업 스냅샷 (2026-09-17)

- 사용자 Git 작업 요청에 따라 첫 커밋 이후 누적된 네이티브 앱, 작업 화면·저장/복구, 모델 선택, SOAP 분할 시험 및 문서 정리를 로컬 커밋 대상으로 점검했다. 기존 변경을 보존했으며 모델 품질 미달·Cloud Pro 한도·STT-2 중단 상태를 완료로 바꾸지 않았다.
- 현재 코드로 Python 회귀 78개 통과, Swift 단위 테스트 3개 통과/통합 테스트 1개 명시적 skip, 임시 DB·모의 모델을 사용한 DOM 회귀 통과. Swift 시험은 별도 패키지 빌드이며 실행 중인 앱을 교체하지 않았다. 외부 모델 호출·GUI 시험·배포·재시작은 이번 Git 검증에 포함하지 않았다.
- 커밋 후보 소스 65개 파일의 Gitleaks 검사에서 검출 없음. DB·음원·전사 결과·시험 산출물·자격증명·Xcode 개인 상태와 빌드 파일은 제외한다. 로그는 Git 제외 `.build/git-checkpoint/`에 보관한다. 원격 저장소는 등록되어 있지 않으며 이번 범위는 로컬 커밋이다.

## N6 — 앱 이름 변경 (2026-09-17)

- 사용자 최종 지정에 따라 앱 이름을 `한의원AI로컬구축.app`으로 변경했다. Info.plist 표시 이름, 창·사이드바 제목, 사용자 안내, 문자열 카탈로그, Xcode 제품·공유 scheme 및 CLI 빌드 경로를 일치시켰다. Swift 모듈/실행 파일 이름과 bundle ID `com.raphael.ClinicAI`는 유지한다.
- Xcode Release 빌드 성공. 최종 앱 `.build/native/한의원AI로컬구축.app`의 서명 검증 통과, 이전 앱과 designated requirement 및 Runtime.json(DB·Python·포트)이 동일함을 확인했다. 구 앱은 Git 제외 `.build/app-rename/previous.app`에 보존했다. PRD의 오래된 ad-hoc 서명 설명도 현재 고정 개발자 서명과 일치시켰다.
- 이름 변경만으로 업무 기능/모델 품질을 재검증하지 않았다. 실행 중인 앱은 재시작하지 않았으므로 창·메뉴의 새 이름은 새 앱을 다음 실행할 때 적용된다. TCC 권한의 실제 재실행 유지 여부는 이번에 시험하지 않았다. 검증 증거는 `.build/app-rename/verification.json`, `build.log`다.

## G2 — 한국어 README와 GitHub 게시 (2026-09-17)

- 사용자 요청으로 README.md에 기능, 아키텍처, 환경 요구, CLI/Xcode별 DB·포트, 서명 설정, 로컬/Cloud 처리 범위, 검증 명령 및 미완료 품질 항목을 정리했다. 앱의 최종 이름 변경도 함께 커밋 대상으로 포함했다.
- 기존 원격이 없어 인증된 Raphael-KR 계정에 비공개 `clinic-ai` 저장소를 생성하고 origin을 연결했다. 공개 전환·Release 배포는 범위에 포함하지 않는다. 게시 대상은 소스·문서·테스트이며 기존 Git 이력의 Gitleaks 검사에서도 검출 항목이 없었다.
- README의 상대 문서 링크와 앱 이름을 검사했다. 실행 코드의 추가 기능 변경이 없어 N6 빌드/서명과 G1 회귀 결과를 근거로 사용한다. push 후 원격 main과 로컬 HEAD의 일치 및 작업 트리 상태를 별도 확인한다.

## G3 — 실험용 목적과 AFM 상세 결과 공개 설명 (2026-09-17)

- 사용자 요청대로 README 첫머리에 Notion AI 자동화 링크를 Codex에게 전달해 빠르게 만든 프로젝트라는 배경, 앱은 동작하지만 실전용 AI 품질은 미달이라는 상태, macOS 로컬 LLM 테스트 용도를 명시했다.
- C5·Q1·Q2~Q5·S1~S2 기록과 로컬 실험 JSON을 대조해 호출 경로, Core 미시험, 구조/내용 통과 구분, 오류 사례, 4회 분할 loop·39회 시도, 호출량·지연, Cloud Pro 한도, 용도별 한계와 평가 방법을 README에 정리했다. 용도 제안은 시험에서 도출한 판단이며 해당 분야별 통과 결과가 아니라고 구분했다.
- 문서 링크·코드블록, Q1 18회와 모델별 평균, 최종 분할 성공 건수를 검사했다. 최종 분할 실패는 최상위 키가 아니라 result.error에 들어 있으므로 result.soap 존재 기준으로 성공 건수를 대조했다. 제품 코드 변경·추가 모델 호출·진료 자료 전송 없이 문서만 갱신했다.

## G4 — AFM 3 공식 소개와 모델/API 구분 (2026-09-17)

- 사용자 제공 Apple Machine Learning Research 소개 및 Apple의 macOS 27 배포 공지를 확인하여 README에 출처 링크를 포함한 모델 소개를 추가했다. 모델 선택은 로컬 LLM인 AFM 3 Core Advanced로 표현하고 Foundation Models 프레임워크와 구분했다.
- 공개 모델 규모·실행 위치·자체 평가를 요약하되 공식 소개와 이 앱의 실제 시험 결과를 별도 절로 유지했다. 공개되지 않은 규모를 추정하지 않았고 새 모델 시험이나 구현 변경은 수행하지 않았다. 문서 링크·표기·diff 검사 후 GitHub main에 반영한다.

## G5 — 다른 Mac의 빌드 조건과 Apple 제공 Python 확인 (2026-09-17)

- README에 macOS 27·Xcode 27 기반 개발자용 소스라는 점과 다른 Mac에서 필요한 서명·Command Line Tools/SDK 경로 조정을 추가했다. 독립 배포용 앱 실행 조건과 현재 소스 빌드 조건을 구분했다.
- Python 공식 macOS 문서와 이 Mac의 실행 경로를 확인했다. `/usr/bin/python3`는 Xcode 개발 도구의 Python 3.9.6으로 연결되고 기본 셸은 Homebrew Python 3.14.7을 사용한다. Apple 제공 Python으로 서버 import 및 전체 Python 회귀 78개를 실행해 8.997초/OK를 확인했다. `.build/git-checkpoint/apple-python-tests.log`.
- 별도 Python 설치를 무조건 필수로 안내하지 않고 설치된 개발 도구 Python의 실행·회귀 확인부터 안내한다. 해당 Python으로 실제 네이티브 생성·전사 전체 흐름을 재시험하지는 않았다. 제품 코드·설치 상태·runtime 설정은 변경하지 않았다.

## G6 — Xcode 에이전트와 함께 시작하는 안내 (2026-09-17)

- 사용자 요청에 따라 Xcode 27의 Agents에서 Codex 또는 Claude Agent를 연결하고 프로젝트 문서를 읽힌 뒤 서명·도구 경로·시험 DB를 조정해 빌드·검증하는 권장 순서를 README에 추가했다. Apple 공식 설정 문서 및 OpenAI 인증 안내를 확인해 링크했다.
- 개발 에이전트와 앱의 AFM 생성 모델, Agents와 일반 Chat 설정을 구분했다. 이번 작업은 안내 문서 수정이며 다른 계정 로그인·에이전트 설치·프로젝트 설정 변경은 하지 않았다.

## G7 — 공개 저장소 준비 및 개인정보 정리 (2026-09-17)

- 사용자 요청: GitHub 공개용 준비와 비밀번호·개인정보·진료 자료 유출 점검. 기존 비공개 main의 8개 커밋을 Gitleaks로 검사했으며 비밀정보 검출은 0건이었다. 자동 검출 결과와 개인정보 검토를 구분했다.
- 수동 검토에서 개인 커밋 이메일, 홈 경로, Notion 원본 페이지·DB·속성 식별자와 계정명, 개인 서명 인증서 지문·팀 ID를 발견했다. 공개 파일에서 제거하고 관계 스키마에는 `collection://local-<table>` 식별자를 사용한다. 앱의 공개 bundle ID는 기존 권한 호환성을 위해 유지한다.
- 기존 원격은 비공개 보관본으로 유지하고 별도의 공개 이력을 정리한 스냅샷에서 시작한다. 공개 이력의 작성자 이메일은 GitHub noreply 주소를 사용한다. 원본 Git bundle·추적 파일 사본과 검사 로그는 Git 제외 `.build/public-audit/`에 보존한다.
- 개인 서명값을 Git 제외 `macos/Signing.local.xcconfig`로 옮겼다. CLI와 Xcode가 같은 파일을 읽으며 공개 예제만 추적한다. 인증서가 없을 때 ad-hoc 서명으로 자동 전환하지 않는다. DB·오디오·자격증명·개인 Xcode 설정·압축 백업 제외 패턴을 보강했다.
- 정리 후 Python 78개(8.815초), Swift 단위 3개, DOM 회귀 통과. 운영 서비스에 쓰는 Swift 통합 시험 1개는 제외했다. 관계 스키마의 모든 참조가 로컬 테이블로 연결됨을 확인했다. Xcode Release 및 격리 사본의 CLI 빌드·서명 검증 통과. 기존 앱과 두 빌드의 designated requirement가 동일했다. 실행 중인 앱·DB·권한을 변경하지 않았다.
- 추적 파일에서 원래의 비공개 식별자 재검출 0건. 전화번호 예시는 합성값만 있으며 실제 환자 DB·음원·전사 파일은 추적 대상에 없다. 유일한 추적 바이너리는 생성한 앱 아이콘이다. `.gitignore`, 셸 문법, Xcode 프로젝트 형식과 diff 검사를 통과했다. 원격 게시·재복제 검증 결과는 후속 기록으로 남긴다.

### G7 원격 공개 및 후속 확인

- 개인정보를 제거한 첫 스냅샷을 새 독립 저장소에 올린 뒤 다시 clone했다. 68개 파일의 바이트 일치, 공개 이력 1개 커밋, 원래 비공개 식별자 0건, Gitleaks 비밀정보 0건을 확인한 후 공개로 전환했다. 기존 저장소는 이름을 바꾸어 비공개 상태와 기존 main을 보존했다. 원본 이력을 새 저장소로 force-push하거나 기존 저장소를 삭제하지 않았다.
- 로컬 작업 폴더도 공개 이력의 main/origin을 사용하도록 연결했다. 이전 `.git` 전체는 `.build/public-audit/original.git`에 보존했다. 새 커밋은 개인 이메일 대신 GitHub noreply 주소를 사용한다.
- 인증 없는 HTTP 검사: 공개 저장소·README 200, 비공개 보관본 404, 공개 저장소의 이전 커밋 조회는 422(No commit found). 최초 검사에서 해당 응답을 404로만 예상해 assertion이 실패했고, GitHub의 실제 오류 본문을 확인하여 ‘커밋 없음’으로 검증했다.
- GitHub Secret scanning과 Push protection 활성 상태를 API로 확인했다. 조회 시점의 secret alert는 0건. 일반 비밀번호 패턴 옵션은 활성화 요청 후에도 disabled로 반환되어 활성이라고 보고하지 않는다. 개인정보 전수 검출이나 미래의 유출 방지를 보장하지 않으며, 자료 추가 때 스테이징 내용을 확인한다.
- 공개 범위는 소스·문서·테스트다. 앱 배포·공증·실제 AFM 품질 재시험은 이번 작업에 포함하지 않았다. 기존 DB와 실행 중 앱은 유지했다. 검사 산출물은 Git 제외 `.build/public-audit/`에 있다.
