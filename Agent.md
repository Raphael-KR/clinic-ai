# Clinic AI 작업 지침

- 프로젝트 루트는 `저장소 루트`다. 작업 시작 시 `$HOME/AGENTS.md`와 이 파일을 읽는다. 파일명은 사용자 지정 `Agent.md`로 유지한다.
- 현재 사용자 요청이 이전 설계와 기록보다 우선한다. 현재 제품 명세는 `PRD.md`, 최근 검증과 잔여 한계는 `history.md`다. 작업 중심 문서는 이 세 파일만 유지한다.
- 단일 사용자 로컬 MVP다. `macos/`의 SwiftUI 앱이 새 직원 UI다. `clinic_ai.app`은 기존 데이터·AI 로직을 제공하는 로컬 서버이며 기본 포트는 8766이다. 네이티브 전환 검증 전 웹·DB를 삭제하지 않는다. 현재 시험 DB와 기본 빈 DB를 혼동하지 않도록 PRD의 실행 명령을 따른다.
- 로컬 생성·저장은 동의·검토·승인 상태로 차단하지 않는다. 요청 출처, 입력 형식, 관계 무결성, revision 충돌 검사는 유지한다.
- AI 기본은 Apple Foundation Models 로컬이며 사용자가 선택하면 검증된 Cloud/Cloud Pro 단축어로 호출한다. 자동 모델 fallback은 없다. 전사는 Apple Speech API를 사용한다. 마이크 PCM은 메모리와 파이프로만 전달하며 오디오 파일을 만들지 않는다. 기존 녹음 파일 업로드 기능은 유지한다.
- AI 결과는 한의사 검토용 초안이다. 모델의 임상 정확도를 기능 테스트 통과로 대신하지 않는다.
- Notion은 구조·스킬의 참조 원천이다. 외부 Notion·Sigma·카카오 변경이나 발송은 현재 앱 범위가 아니다.
- DB, 전사 원문, 시험 자료, 오디오, 자격증명, 빌드 산출물은 Git에 넣지 않는다. 기존 사용자 데이터는 정리 대상 코드와 구분하여 보존한다.
- 오래된 구현과 조사 기록은 Git 이력으로 찾는다. 현재 문서에 폐기된 실행 방법과 중복 변경일지를 누적하지 않는다. 실제로 사용 중인 스키마·AI 스킬 콘텐츠와 회귀 테스트는 보존한다.
- CLI/API를 우선한다. GUI는 해당 작업에 대한 명시적 승인을 받은 범위에서만 사용한다. 별도 요청 없이 다른 작업이나 하위 에이전트를 만들지 않는다.
- 변경 전 Git 상태를 확인하고 기존 변경을 보존한다. 변경 후 관련 테스트와 필요 시 실제 런타임을 검증하고 `history.md`에 결과를 기록한다. 과거 검증을 이번 실행 결과로 표현하지 않는다.
